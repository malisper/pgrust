# Aristo — project verification configuration
#
# Scaffolded by Aretta onboarding. Fill in the TODO fields below and commit;
# everything here is safe to edit. See ./README.md for the working model.

[project]
# TODO: a short, human-friendly name for this project (shown on your dashboard).
name = "pgrust-fast"

# TODO: the branch Aretta verifies. Usually your default branch, e.g. "main".
default_branch = "main"

[verification]
# TODO: this repo's toolchain, e.g. "rust", "python", "go". Aretta uses it to
# build and exercise your code inside an isolated sandbox.
toolchain = "rust"

# Path globs holding the code you want verified.
# TODO: narrow this to the packages/crates that matter.
include = ["crates/**"]

[contact]
# TODO: where Aretta should reach you — a GitHub team handle or an email.
owner = "malis@pagerfree.com"
