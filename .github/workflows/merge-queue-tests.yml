name: merge-queue-tests

on:
  pull_request:
    branches:
      - perf-optimization
  merge_group:
    branches:
      - perf-optimization
  workflow_dispatch:

permissions:
  contents: read

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: "1"
  TOTAL_SHARDS: "4"

jobs:
  cargo-test-shard:
    name: cargo-test-shard-${{ matrix.shard }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        shard: ["1", "2", "3", "4"]
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Install cargo-nextest
        uses: taiki-e/install-action@nextest

      - name: Restore cargo cache
        uses: Swatinem/rust-cache@v2

      - name: Run sharded cargo tests
        run: |
          mkdir -p target/nextest
          cargo nextest run \
            --locked \
            --lib \
            --partition "hash:${{ matrix.shard }}/${TOTAL_SHARDS}" \
            --junit-report "target/nextest/junit-shard-${{ matrix.shard }}.xml"

      - name: Upload JUnit timing report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: junit-shard-${{ matrix.shard }}
          path: target/nextest/junit-shard-${{ matrix.shard }}.xml
          if-no-files-found: error
          retention-days: 14

  cargo-test:
    name: cargo-test
    runs-on: ubuntu-latest
    needs:
      - cargo-test-shard
    if: always()
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Download JUnit timing reports
        uses: actions/download-artifact@v4
        with:
          path: artifacts
          pattern: junit-shard-*
          merge-multiple: false

      - name: Summarize test timings
        run: |
          python3 scripts/github/render_nextest_timings.py artifacts/*/*.xml >>"$GITHUB_STEP_SUMMARY"

      - name: Fail if any shard failed
        env:
          NEEDS_JSON: ${{ toJson(needs) }}
        run: |
          python3 - <<'PY'
          import json
          import os
          import sys

          needs = json.loads(os.environ["NEEDS_JSON"])
          failed = {
              job_name: info["result"]
              for job_name, info in needs.items()
              if info["result"] != "success"
          }
          if failed:
              print("One or more test shards did not succeed:", file=sys.stderr)
              for job_name, result in failed.items():
                  print(f"  {job_name}: {result}", file=sys.stderr)
              sys.exit(1)
          PY
