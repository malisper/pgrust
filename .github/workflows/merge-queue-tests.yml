name: merge-queue-tests

on:
  pull_request:
    branches:
      - perf-optimization
  merge_group:
    branches:
      - perf-optimization
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: read

env:
  CARGO_TERM_COLOR: always
  CARGO_PROFILE_TEST_OPT_LEVEL: "3"
  RUST_BACKTRACE: "1"
  # .cargo/config.toml sets target-dir = /tmp/pgrust-target so multiple local
  # worktrees can share build artifacts. In CI that path makes Swatinem/rust-cache
  # cache an empty ./target directory (it captured 23 MB of registry junk and
  # zero compiled artifacts). Override here so cargo builds where the cache
  # action expects.
  CARGO_TARGET_DIR: target

jobs:
  changes:
    name: detect-changes
    runs-on: ubuntu-latest
    outputs:
      code: ${{ github.event_name == 'workflow_dispatch' && 'true' || steps.filter.outputs.code }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Detect code changes
        id: filter
        if: ${{ github.event_name != 'workflow_dispatch' }}
        uses: dorny/paths-filter@v4
        with:
          base: ${{ github.event.merge_group.base_sha || github.event.pull_request.base.sha || '' }}
          ref: ${{ github.event.merge_group.head_sha || github.event.pull_request.head.sha || github.ref }}
          filters: |
            code:
              - '**/*.rs'
              - 'Cargo.toml'
              - 'Cargo.lock'
              - 'rust-toolchain.toml'
              - 'rustfmt.toml'
              - '.config/**'
              - '.github/workflows/**'
              - 'scripts/**'
              - 'build.rs'

  cargo-fmt-check:
    name: cargo-fmt-check
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: 1.94.1
          components: rustfmt

      - name: Check formatting
        run: cargo fmt --all -- --check

  cargo-test-archive:
    name: cargo-test-archive
    needs:
      - changes
      - cargo-fmt-check
    if: ${{ github.event_name != 'pull_request' && needs.changes.outputs.code == 'true' }}
    runs-on:
      group: large-runners-like-32x-paid
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: 1.94.1

      - name: Install cargo-nextest
        uses: taiki-e/install-action@nextest

      - name: Restore cargo cache
        uses: Swatinem/rust-cache@v2
        with:
          # Default save-if only fires when github.ref matches the default branch.
          # Merge_group runs are on ephemeral gh-readonly-queue/* branches, so the
          # default never matches and the cache is never saved. Save additionally
          # on workflow_dispatch (manual benchmarks) so we can populate the cache
          # for future merge_group runs to restore from.
          save-if: ${{ github.ref == 'refs/heads/perf-optimization' || github.event_name == 'workflow_dispatch' }}

      - name: Build nextest archive
        run: |
          mkdir -p target/nextest
          cargo nextest archive \
            --locked \
            --lib \
            --profile ci \
            --config-file .config/nextest.toml \
            --archive-file target/nextest/pgrust-tests.tar.zst

      - name: Upload nextest archive
        uses: actions/upload-artifact@v4
        with:
          name: nextest-archive
          path: target/nextest/pgrust-tests.tar.zst
          if-no-files-found: error
          retention-days: 1

  cargo-test-run:
    name: cargo-test-run (${{ matrix.partition }}/4)
    needs:
      - changes
      - cargo-fmt-check
      - cargo-test-archive
    if: ${{ github.event_name != 'pull_request' && needs.changes.outputs.code == 'true' }}
    runs-on:
      group: large-runners-like-32x-paid
    strategy:
      fail-fast: false
      matrix:
        partition: [1, 2, 3, 4]
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: 1.94.1

      - name: Install cargo-nextest
        uses: taiki-e/install-action@nextest

      - name: Download nextest archive
        uses: actions/download-artifact@v4
        with:
          name: nextest-archive
          path: target/nextest/archive

      - name: Run cargo tests shard
        run: |
          cargo nextest run \
            --archive-file target/nextest/archive/pgrust-tests.tar.zst \
            --profile ci \
            --config-file .config/nextest.toml \
            --partition count:${{ matrix.partition }}/4

      - name: Upload JUnit timing report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: junit-report-${{ matrix.partition }}
          path: target/nextest/ci/junit.xml
          if-no-files-found: error
          retention-days: 14

  cargo-test:
    name: cargo-test
    runs-on: ubuntu-latest
    needs:
      - changes
      - cargo-fmt-check
      - cargo-test-archive
      - cargo-test-run
    if: always()
    steps:
      - name: Pass quickly on pull requests
        if: ${{ github.event_name == 'pull_request' }}
        run: |
          echo "PR-time cargo-test is intentionally a no-op."

      - name: Pass quickly on docs-only merges
        if: ${{ github.event_name != 'pull_request' && needs.changes.outputs.code != 'true' }}
        run: |
          echo "No code changes detected; skipping test aggregation."

      - name: Checkout
        if: ${{ github.event_name != 'pull_request' && needs.changes.outputs.code == 'true' }}
        uses: actions/checkout@v4

      - name: Download JUnit timing reports
        if: ${{ github.event_name != 'pull_request' && needs.changes.outputs.code == 'true' }}
        uses: actions/download-artifact@v4
        with:
          path: artifacts
          pattern: junit-report-*
          merge-multiple: false

      - name: Summarize test timings
        if: ${{ github.event_name != 'pull_request' && needs.changes.outputs.code == 'true' }}
        run: |
          python3 scripts/github/render_nextest_timings.py artifacts/*/*.xml >>"$GITHUB_STEP_SUMMARY"

      - name: Fail if any required job failed
        env:
          NEEDS_JSON: ${{ toJson(needs) }}
          EVENT_NAME: ${{ github.event_name }}
          CODE_CHANGED: ${{ needs.changes.outputs.code }}
        run: |
          python3 - <<'PY'
          import json
          import os
          import sys

          needs = json.loads(os.environ["NEEDS_JSON"])
          event_name = os.environ["EVENT_NAME"]
          code_changed = os.environ["CODE_CHANGED"] == "true"

          # On pull_request, cargo-test-archive/cargo-test-run are intentionally skipped.
          # On docs-only merges, test jobs are intentionally skipped.
          # In either case, "skipped" is an expected result, not a failure.
          allowed_skipped = set()
          if event_name == "pull_request":
              allowed_skipped.add("cargo-test-archive")
              allowed_skipped.add("cargo-test-run")
          if not code_changed:
              allowed_skipped.add("cargo-test-archive")
              allowed_skipped.add("cargo-test-run")

          failed = {}
          for job_name, info in needs.items():
              result = info["result"]
              if result == "success":
                  continue
              if result == "skipped" and job_name in allowed_skipped:
                  continue
              failed[job_name] = result
          if failed:
              print("The test workflow did not succeed:", file=sys.stderr)
              for job_name, result in failed.items():
                  print(f"  {job_name}: {result}", file=sys.stderr)
              sys.exit(1)
          PY
