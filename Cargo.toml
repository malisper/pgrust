[package]
name = "pgrust"
version = "0.1.0"
edition = "2024"

[lib]
name = "pgrust"
path = "src/lib.rs"

[dependencies]
libc = "0.2"
parking_lot = { version = "0.12", features = ["deadlock_detection"] }
pest = "2.7"
pest_derive = "2.7"
rand = "0.8"
