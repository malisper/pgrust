//! simharness library surface. WS-RUNNER declares `runner`; WS-GEN adds
//! `pub mod plan;` + `pub mod gen;` and WS-ORACLE adds `pub mod oracle;` +
//! `pub mod vocab;` at integration (contract §1.1 layout).

pub mod runner;
