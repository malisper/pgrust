[package]
name = "pg_ffi_fgram"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "pg_ffi_fgram"
path = "src/lib.rs"

[dependencies]
types_core = { path = "../../types/types_core" }
