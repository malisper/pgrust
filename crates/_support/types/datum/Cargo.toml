[package]
name = "datum"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "datum"
path = "src/lib.rs"

[dependencies]
mcx = { path = "../../mcx" }
types_core = { path = "../types_core" }
