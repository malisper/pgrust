[package]
name = "hash"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "hash"
path = "src/lib.rs"

[dependencies]
mcx = { path = "../../mcx" }
types_amvalidate = { path = "../types_amvalidate" }
types_core = { path = "../types_core" }
types_storage = { path = "../types_storage" }
types_tuple = { path = "../types_tuple" }
