[package]
name = "parse_manifest"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "parse_manifest"
path = "src/lib.rs"

[dependencies]
types_core = { path = "../types_core" }
