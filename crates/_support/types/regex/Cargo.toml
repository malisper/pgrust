[package]
name = "regex"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "regex"
path = "src/lib.rs"

[dependencies]
types_core = { path = "../types_core" }
mcx = { path = "../../mcx" }
