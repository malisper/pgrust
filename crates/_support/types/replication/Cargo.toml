[package]
name = "replication"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "replication"
path = "src/lib.rs"

[dependencies]
types_core = { path = "../types_core" }
parsenodes = { path = "../parsenodes" }
types_datetime = { path = "../types_datetime" }
