[package]
name = "replication_applyparallel"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "replication_applyparallel"
path = "src/lib.rs"

[dependencies]
types_core = { path = "../types_core" }
