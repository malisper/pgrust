[package]
name = "replication_launcher"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "replication_launcher"
path = "src/lib.rs"

[dependencies]
types_core = { path = "../types_core" }
