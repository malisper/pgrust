[package]
name = "replication_slot_2"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "replication_slot_2"
path = "src/lib.rs"

[dependencies]
types_core = { path = "../types_core" }
types_tuple = { path = "../types_tuple" }
