[package]
name = "tsearch"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "tsearch"
path = "src/lib.rs"

[dependencies]
types_core = { path = "../types_core" }
gin = { path = "../gin" }
mcx = { path = "../../mcx" }
