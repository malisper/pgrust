[package]
name = "types_amapi"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "types_amapi"
path = "src/lib.rs"

[dependencies]
types_tableam = { path = "../types_tableam" }
