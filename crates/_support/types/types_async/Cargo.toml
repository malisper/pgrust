[package]
name = "types_async"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "types_async"
path = "src/lib.rs"

[dependencies]
types_core = { path = "../types_core" }
