[package]
name = "types_format_type"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "types_format_type"
path = "src/lib.rs"

[dependencies]
mcx = { path = "../../mcx" }
types_core = { path = "../types_core" }
