[package]
name = "types_freepage"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "types_freepage"
path = "src/lib.rs"

[dependencies]
types_core = { path = "../types_core" }
