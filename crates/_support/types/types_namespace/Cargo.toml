[package]
name = "types_namespace"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "types_namespace"
path = "src/lib.rs"

[dependencies]
mcx = { path = "../../mcx" }
types_core = { path = "../types_core" }
