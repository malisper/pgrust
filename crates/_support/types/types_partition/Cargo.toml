[package]
name = "types_partition"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "types_partition"
path = "src/lib.rs"

[dependencies]
types_core = { path = "../types_core" }
types_error = { path = "../types_error" }
nodes = { path = "../nodes" }
mcx = { path = "../../mcx" }
