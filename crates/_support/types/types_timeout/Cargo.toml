[package]
name = "types_timeout"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "types_timeout"
path = "src/lib.rs"

[dependencies]
types_core = { path = "../types_core" }
