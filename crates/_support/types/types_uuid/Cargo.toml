[package]
name = "types_uuid"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "types_uuid"
path = "src/lib.rs"

[dependencies]
mcx = { path = "../../mcx" }
nodes = { path = "../nodes" }
