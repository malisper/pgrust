[package]
name = "wal"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "wal"
path = "src/lib.rs"

[dependencies]
mcx = { path = "../../mcx" }
types_core = { path = "../types_core" }
types_error = { path = "../types_error" }
types_storage = { path = "../types_storage" }
types_logical = { path = "../types_logical" }
