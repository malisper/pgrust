[package]
name = "backend-access-gin-core-probe-seams"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "backend_access_gin_core_probe_seams"
path = "src/lib.rs"

[dependencies]
seam-core = { path = "../seam-core" }
types-tsearch = { path = "../types-tsearch" }
