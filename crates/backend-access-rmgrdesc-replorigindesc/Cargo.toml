[package]
name = "backend-access-rmgrdesc-replorigindesc"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "backend_access_rmgrdesc_replorigindesc"
path = "src/lib.rs"

[dependencies]
mcx = { path = "../mcx" }
types-core = { path = "../types-core" }
types-error = { path = "../types-error" }
types-wal = { path = "../types-wal" }
# This crate's own inward seam (replorigin_desc / replorigin_identify slots).
backend-access-rmgrdesc-replorigindesc-seams = { path = "../backend-access-rmgrdesc-replorigindesc-seams" }
