[package]
name = "backend-access-rmgrdesc-smgrdesc"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "backend_access_rmgrdesc_smgrdesc"
path = "src/lib.rs"

[dependencies]
mcx = { path = "../mcx" }
types-core = { path = "../types-core" }
types-error = { path = "../types-error" }
types-storage = { path = "../types-storage" }
types-wal = { path = "../types-wal" }
# This crate's own inward seam (smgr_desc / smgr_identify rm_desc slots).
backend-access-rmgrdesc-smgrdesc-seams = { path = "../backend-access-rmgrdesc-smgrdesc-seams" }
# relpathperm == relpathbackend(rlocator, INVALID_PROC_NUMBER, forknum); owner
# common/relpath.c unported, so through its per-owner seam.
common-relpath-seams = { path = "../common-relpath-seams" }
