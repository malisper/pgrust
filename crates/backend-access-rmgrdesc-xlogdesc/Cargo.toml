[package]
name = "backend-access-rmgrdesc-xlogdesc"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "backend_access_rmgrdesc_xlogdesc"
path = "src/lib.rs"

[dependencies]
mcx = { path = "../mcx" }
types-core = { path = "../types-core" }
types-error = { path = "../types-error" }
types-wal = { path = "../types-wal" }
# This crate's own inward seam (xlog_desc / xlog_identify rm_desc slots).
backend-access-rmgrdesc-xlogdesc-seams = { path = "../backend-access-rmgrdesc-xlogdesc-seams" }
# timestamptz_to_str (END_OF_RECOVERY / OVERWRITE_CONTRECORD); owner
# utils/adt/timestamp.c unported, through its seam.
backend-utils-adt-timestamp-seams = { path = "../backend-utils-adt-timestamp-seams" }
