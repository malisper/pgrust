[package]
name = "backend-access-spg-kdtree"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "backend_access_spg_kdtree"
path = "src/lib.rs"

[dependencies]
types-core = { path = "../types-core" }
types-error = { path = "../types-error" }
backend-utils-adt-geo-ops-seams = { path = "../backend-utils-adt-geo-ops-seams" }
backend-access-spg-proc-seams = { path = "../backend-access-spg-proc-seams" }
