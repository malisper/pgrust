[package]
name = "backend-access-tablesample-core-seams"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "backend_access_tablesample_core_seams"
path = "src/lib.rs"

[dependencies]
seam-core = { path = "../seam-core" }
types-error = { path = "../types-error" }
types-core = { path = "../types-core" }
types-datum = { path = "../types-datum" }
types-samplescan = { path = "../types-samplescan" }
mcx = { path = "../mcx" }
