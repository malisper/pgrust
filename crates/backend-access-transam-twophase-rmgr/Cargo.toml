[package]
name = "backend-access-transam-twophase-rmgr"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "backend_access_transam_twophase_rmgr"
path = "src/lib.rs"

[dependencies]
types-core = { path = "../types-core" }
types-error = { path = "../types-error" }
# Every non-NULL table slot is a callback owned by another subsystem; each is
# reached through its owner's seam crate (the owners install on landing).
backend-storage-lmgr-lock-seams = { path = "../backend-storage-lmgr-lock-seams" }
backend-access-transam-multixact-seams = { path = "../backend-access-transam-multixact-seams" }
backend-utils-activity-stat-seams = { path = "../backend-utils-activity-stat-seams" }
backend-storage-lmgr-predicate-seams = { path = "../backend-storage-lmgr-predicate-seams" }
