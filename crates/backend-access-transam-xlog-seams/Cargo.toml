[package]
name = "backend-access-transam-xlog-seams"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "backend_access_transam_xlog_seams"
path = "src/lib.rs"

[dependencies]
seam-core = { path = "../seam-core" }
