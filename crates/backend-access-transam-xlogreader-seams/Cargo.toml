[package]
name = "backend-access-transam-xlogreader-seams"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "backend_access_transam_xlogreader_seams"
path = "src/lib.rs"

[dependencies]
seam-core = { path = "../seam-core" }
types-logical = { path = "../types-logical" }
types-core = { path = "../types-core" }
