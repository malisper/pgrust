[package]
name = "backend-access-transam-xlogrecovery-seams"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "backend_access_transam_xlogrecovery_seams"
path = "src/lib.rs"

[dependencies]
seam-core = { path = "../seam-core" }
types-core = { path = "../types-core" }
types-wal = { path = "../types-wal" }
mcx = { path = "../mcx" }
types-error = { path = "../types-error" }
