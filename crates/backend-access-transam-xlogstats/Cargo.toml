[package]
name = "backend-access-transam-xlogstats"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "backend_access_transam_xlogstats"
path = "src/lib.rs"

[dependencies]
types-core = { path = "../types-core" }
types-wal = { path = "../types-wal" }

[dev-dependencies]
mcx = { path = "../mcx" }
