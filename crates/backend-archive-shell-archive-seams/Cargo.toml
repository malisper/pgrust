[package]
name = "backend-archive-shell-archive-seams"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "backend_archive_shell_archive_seams"
path = "src/lib.rs"

[dependencies]
seam-core = { path = "../seam-core" }
types-pgarch = { path = "../types-pgarch" }
