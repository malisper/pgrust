[package]
name = "backend-archive-shell-archive"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "backend_archive_shell_archive"
path = "src/lib.rs"

[dependencies]
backend-archive-shell-archive-seams = { path = "../backend-archive-shell-archive-seams" }
types-pgarch = { path = "../types-pgarch" }
types-error = { path = "../types-error" }
backend-utils-error = { path = "../backend-utils-error" }
mcx = { path = "../mcx" }
common-percentrepl = { path = "../common-percentrepl" }
common-wait-error-seams = { path = "../common-wait-error-seams" }
backend-utils-activity-waitevent-seams = { path = "../backend-utils-activity-waitevent-seams" }
backend-access-transam-xlog-seams = { path = "../backend-access-transam-xlog-seams" }
backend-postmaster-pgarch = { path = "../backend-postmaster-pgarch" }
libc = "0.2"

[target.'cfg(target_family = "wasm")'.dependencies]
wasm-libc-shim = { path = "../wasm-libc-shim" }
