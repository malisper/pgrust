[package]
name = "backend-backup-lz4-seams"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "backend_backup_lz4_seams"
path = "src/lib.rs"

[dependencies]
seam-core = { path = "../seam-core" }
