[package]
name = "backend-backup-sink"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "backend_backup_sink"
path = "src/lib.rs"

[dependencies]
mcx = { path = "../mcx" }
types-core = { path = "../types-core" }
types-error = { path = "../types-error" }
