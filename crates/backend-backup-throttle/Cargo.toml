[package]
name = "backend-backup-throttle"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "backend_backup_throttle"
path = "src/lib.rs"

[dependencies]
mcx = { path = "../mcx" }
types-core = { path = "../types-core" }
types-error = { path = "../types-error" }
types-datetime = { path = "../types-datetime" }
types-pgstat = { path = "../types-pgstat" }
types-storage = { path = "../types-storage" }
backend-backup-sink = { path = "../backend-backup-sink" }
backend-backup-throttle-seams = { path = "../backend-backup-throttle-seams" }
backend-storage-ipc-latch-seams = { path = "../backend-storage-ipc-latch-seams" }

[dev-dependencies]
seam-core = { path = "../seam-core" }
