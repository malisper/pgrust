[package]
name = "backend-catalog-pg-enum-seams"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "backend_catalog_pg_enum_seams"
path = "src/lib.rs"

[dependencies]
seam-core = { path = "../seam-core" }
