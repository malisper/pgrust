[package]
name = "backend-catalog-pg-subscription-seams"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "backend_catalog_pg_subscription_seams"
path = "src/lib.rs"

[dependencies]
seam-core = { path = "../seam-core" }
types-error = { path = "../types-error" }
types-replication-launcher = { path = "../types-replication-launcher" }
mcx = { path = "../mcx" }
