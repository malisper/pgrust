[package]
name = "backend-commands-async-seams"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "backend_commands_async_seams"
path = "src/lib.rs"

[dependencies]
types-core = { path = "../types-core" }
seam-core = { path = "../seam-core" }
types-error = { path = "../types-error" }
