[package]
name = "backend-commands-collationcmds-seams"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "backend_commands_collationcmds_seams"
path = "src/lib.rs"

[dependencies]
seam-core = { path = "../seam-core" }
types-core = { path = "../types-core" }
types-error = { path = "../types-error" }
mcx = { path = "../mcx" }
