[package]
name = "backend-commands-event-trigger-fire-seams"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "backend_commands_event_trigger_fire_seams"
path = "src/lib.rs"

[dependencies]
seam-core = { path = "../seam-core" }
types-error = { path = "../types-error" }
types-evtcache = { path = "../types-evtcache" }
types-nodes = { path = "../types-nodes" }
