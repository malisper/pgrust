[package]
name = "backend-commands-user"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "backend_commands_user"
path = "src/lib.rs"
crate-type = ["rlib"]

[dependencies]
backend-utils-error = { path = "../backend-utils-error" }
backend-commands-user-seams = { path = "../backend-commands-user-seams" }
types-core = { path = "../types-core" }
types-error = { path = "../types-error" }
types-nodes = { path = "../types-nodes" }
types-parsenodes = { path = "../types-parsenodes" }
types-authid = { path = "../types-authid" }
types-catalog = { path = "../types-catalog" }
