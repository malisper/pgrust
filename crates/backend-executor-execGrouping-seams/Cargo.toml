[package]
name = "backend-executor-execGrouping-seams"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "backend_executor_execGrouping_seams"
path = "src/lib.rs"

[dependencies]
seam-core = { path = "../seam-core" }
types-error = { path = "../types-error" }
types-nodes = { path = "../types-nodes" }
types-core = { path = "../types-core" }
types-tuple = { path = "../types-tuple" }
types-execgrouping = { path = "../types-execgrouping" }
mcx = { path = "../mcx" }
