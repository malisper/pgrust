[package]
name = "backend-executor-instrument"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "backend_executor_instrument"
path = "src/lib.rs"

[dependencies]
backend-executor-instrument-seams = { path = "../backend-executor-instrument-seams" }
backend-executor-execParallel-support-seams = { path = "../backend-executor-execParallel-support-seams" }
# Lazy-vacuum driver's pgBufferUsage/pgWalUsage snapshot read seams home in
# vacuumlazy-seams; instrument.c owns those backend-global counters.
backend-access-heap-vacuumlazy-seams = { path = "../backend-access-heap-vacuumlazy-seams" }
mcx = { path = "../mcx" }
portability-instr-time = { path = "../portability-instr-time" }
types-core = { path = "../types-core" }
types-error = { path = "../types-error" }
