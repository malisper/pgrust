[package]
name = "backend-executor-nodeWorktablescan"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "backend_executor_nodeWorktablescan"
path = "src/lib.rs"

[dependencies]
seam-core = { path = "../seam-core" }
types-error = { path = "../types-error" }
types-core = { path = "../types-core" }
types-nodes = { path = "../types-nodes" }
backend-executor-nodeWorktablescan-seams = { path = "../backend-executor-nodeWorktablescan-seams" }
mcx = { path = "../mcx" }
