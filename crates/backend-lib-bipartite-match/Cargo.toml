[package]
name = "backend-lib-bipartite-match"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "backend_lib_bipartite_match"
path = "src/lib.rs"
crate-type = ["rlib"]

[dependencies]
mcx = { path = "../mcx" }
types-error = { path = "../types-error" }
backend-tcop-postgres-seams = { path = "../backend-tcop-postgres-seams" }

[dev-dependencies]
