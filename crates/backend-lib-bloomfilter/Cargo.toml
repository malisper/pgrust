[package]
name = "backend-lib-bloomfilter"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "backend_lib_bloomfilter"
path = "src/lib.rs"

[dependencies]
seam-core = { path = "../seam-core" }
types-error = { path = "../types-error" }
backend-lib-bloomfilter-seams = { path = "../backend-lib-bloomfilter-seams" }
common-hashfn-seams = { path = "../common-hashfn-seams" }

[dev-dependencies]
common-hashfn = { path = "../common-hashfn" }
