[package]
name = "backend-lib-dshash-seams"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "backend_lib_dshash_seams"
path = "src/lib.rs"

[dependencies]
seam-core = { path = "../seam-core" }
types-storage = { path = "../types-storage" }
types-error = { path = "../types-error" }
