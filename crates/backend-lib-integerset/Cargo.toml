[package]
name = "backend-lib-integerset"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "backend_lib_integerset"
path = "src/lib.rs"

[dependencies]
types-error = { path = "../types-error" }
mcx = { path = "../mcx" }
