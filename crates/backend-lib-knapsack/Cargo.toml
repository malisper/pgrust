[package]
name = "backend-lib-knapsack"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "backend_lib_knapsack"
path = "src/lib.rs"

[dependencies]
mcx = { path = "../mcx" }
types-error = { path = "../types-error" }
types-nodes = { path = "../types-nodes" }
backend-nodes-core = { path = "../backend-nodes-core" }
