[package]
name = "backend-libpq-auth-scram-seams"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "backend_libpq_auth_scram_seams"
path = "src/lib.rs"

[dependencies]
seam-core = { path = "../seam-core" }
