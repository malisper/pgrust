[package]
name = "backend-libpq-be-secure-common-seams"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "backend_libpq_be_secure_common_seams"
path = "src/lib.rs"

[dependencies]
types-error = { path = "../types-error" }
seam-core = { path = "../seam-core" }
mcx = { path = "../mcx" }
