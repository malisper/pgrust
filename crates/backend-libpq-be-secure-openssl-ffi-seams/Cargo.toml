[package]
name = "backend-libpq-be-secure-openssl-ffi-seams"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "backend_libpq_be_secure_openssl_ffi_seams"
path = "src/lib.rs"

[dependencies]
seam-core = { path = "../seam-core" }
