[package]
name = "backend-nodes-equalfuncs-seams"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "backend_nodes_equalfuncs_seams"
path = "src/lib.rs"

[dependencies]
seam-core = { path = "../seam-core" }
types-nodes = { path = "../types-nodes" }
