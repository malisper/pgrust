[package]
name = "backend-nodes-queryjumble-seams"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "backend_nodes_queryjumble_seams"
path = "src/lib.rs"

[dependencies]
seam-core = { path = "../seam-core" }
types-error = { path = "../types-error" }
types-nodes = { path = "../types-nodes" }
