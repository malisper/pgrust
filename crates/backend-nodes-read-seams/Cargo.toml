[package]
name = "backend-nodes-read-seams"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "backend_nodes_read_seams"
path = "src/lib.rs"

[dependencies]
seam-core = { path = "../seam-core" }
mcx = { path = "../mcx" }
types-error = { path = "../types-error" }
types-nodes = { path = "../types-nodes" }
