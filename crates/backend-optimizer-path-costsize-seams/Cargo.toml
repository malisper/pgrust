[package]
name = "backend-optimizer-path-costsize-seams"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "backend_optimizer_path_costsize_seams"
path = "src/lib.rs"

[dependencies]
seam-core = { path = "../seam-core" }
