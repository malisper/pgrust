[package]
name = "backend-optimizer-path-joinrels-seams"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "backend_optimizer_path_joinrels_seams"
path = "src/lib.rs"

[dependencies]
seam-core = { path = "../seam-core" }
