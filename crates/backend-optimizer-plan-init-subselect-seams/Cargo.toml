[package]
name = "backend-optimizer-plan-init-subselect-seams"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "backend_optimizer_plan_init_subselect_seams"
path = "src/lib.rs"

[dependencies]
seam-core = { path = "../seam-core" }
types-error = { path = "../types-error" }
types-pathnodes = { path = "../types-pathnodes" }
