[package]
name = "backend-optimizer-prep-prepqual"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "backend_optimizer_prep_prepqual"
path = "src/lib.rs"

[dependencies]
seam-core = { path = "../seam-core" }
mcx = { path = "../mcx" }
types-core = { path = "../types-core" }
types-error = { path = "../types-error" }
types-nodes = { path = "../types-nodes" }
types-tuple = { path = "../types-tuple" }
backend-nodes-core = { path = "../backend-nodes-core" }

# Inward seams this unit owns + installs (declared in the clauses.c consumer's
# per-file seam crate; this unit is the prepqual.c C-source owner).
backend-optimizer-prep-prepqual-seams = { path = "../backend-optimizer-prep-prepqual-seams" }

# Outward seams this unit calls.
backend-utils-cache-lsyscache-seams = { path = "../backend-utils-cache-lsyscache-seams" }
backend-nodes-equalfuncs-seams = { path = "../backend-nodes-equalfuncs-seams" }
