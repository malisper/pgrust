[package]
name = "backend-optimizer-prep-preptlist"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "backend_optimizer_prep_preptlist"
path = "src/lib.rs"

[dependencies]
seam-core = { path = "../seam-core" }
mcx = { path = "../mcx" }
types-core = { path = "../types-core" }
types-error = { path = "../types-error" }
types-nodes = { path = "../types-nodes" }
types-pathnodes = { path = "../types-pathnodes" }
types-tuple = { path = "../types-tuple" }
types-rel = { path = "../types-rel" }

# expand_insert_targetlist INSERT leg: open the target relation and build
# missing-column NULL Consts / TargetEntries.
backend-access-table-table = { path = "../backend-access-table-table" }
backend-nodes-core = { path = "../backend-nodes-core" }
# Domain / generated-column legs route through the owners' seams.
backend-parser-coerce-seams = { path = "../backend-parser-coerce-seams" }
backend-utils-cache-lsyscache-seams = { path = "../backend-utils-cache-lsyscache-seams" }

# Inward seam this unit owns + installs (preprocess_targetlist).
backend-optimizer-prep-preptlist-seams = { path = "../backend-optimizer-prep-preptlist-seams" }

# Cross-unit seam this unit owns (get_plan_rowmark, consumed by indxpath as
# `has_plan_rowmark`).
backend-optimizer-util-restrictinfo-seams = { path = "../backend-optimizer-util-restrictinfo-seams" }
