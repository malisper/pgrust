[package]
name = "backend-optimizer-util-relnode-seams"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "backend_optimizer_util_relnode_seams"
path = "src/lib.rs"

[dependencies]
seam-core = { path = "../seam-core" }
types-pathnodes = { path = "../types-pathnodes" }
