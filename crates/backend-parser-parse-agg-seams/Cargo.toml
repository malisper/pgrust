[package]
name = "backend-parser-parse-agg-seams"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "backend_parser_parse_agg_seams"
path = "src/lib.rs"

[dependencies]
seam-core = { path = "../seam-core" }
types-nodes = { path = "../types-nodes" }
