[package]
name = "backend-partitioning-core-seams"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "backend_partitioning_core_seams"
path = "src/lib.rs"

[dependencies]
seam-core = { path = "../seam-core" }
