[package]
name = "backend-postmaster-bgworker-seams"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "backend_postmaster_bgworker_seams"
path = "src/lib.rs"

[dependencies]
seam-core = { path = "../seam-core" }
types-bgworker = { path = "../types-bgworker" }
