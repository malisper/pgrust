[package]
name = "backend-postmaster-bgwriter-seams"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "backend_postmaster_bgwriter_seams"
path = "src/lib.rs"

[dependencies]
seam-core = { path = "../seam-core" }
