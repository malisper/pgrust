[package]
name = "backend-postmaster-startup-seams"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "backend_postmaster_startup_seams"
path = "src/lib.rs"

[dependencies]
seam-core = { path = "../seam-core" }
