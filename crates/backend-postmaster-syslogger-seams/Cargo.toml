[package]
name = "backend-postmaster-syslogger-seams"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "backend_postmaster_syslogger_seams"
path = "src/lib.rs"

[dependencies]
seam-core = { path = "../seam-core" }
types-startup = { path = "../types-startup" }
