[package]
name = "backend-replication-logical-applyparallelworker-seams"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "backend_replication_logical_applyparallelworker_seams"
path = "src/lib.rs"

[dependencies]
seam-core = { path = "../seam-core" }
types-error = { path = "../types-error" }
types-replication-applyparallel = { path = "../types-replication-applyparallel" }
