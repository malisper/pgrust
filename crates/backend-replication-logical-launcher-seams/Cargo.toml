[package]
name = "backend-replication-logical-launcher-seams"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "backend_replication_logical_launcher_seams"
path = "src/lib.rs"

[dependencies]
seam-core = { path = "../seam-core" }
