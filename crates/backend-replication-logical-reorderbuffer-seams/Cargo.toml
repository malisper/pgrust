[package]
name = "backend-replication-logical-reorderbuffer-seams"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "backend_replication_logical_reorderbuffer_seams"
path = "src/lib.rs"

[dependencies]
seam-core = { path = "../seam-core" }
types-logical = { path = "../types-logical" }
