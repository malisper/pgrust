[package]
name = "backend-replication-logical-slotsync-seams"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "backend_replication_logical_slotsync_seams"
path = "src/lib.rs"

[dependencies]
seam-core = { path = "../seam-core" }
types-startup = { path = "../types-startup" }
