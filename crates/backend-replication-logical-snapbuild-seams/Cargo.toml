[package]
name = "backend-replication-logical-snapbuild-seams"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "backend_replication_logical_snapbuild_seams"
path = "src/lib.rs"

[dependencies]
seam-core = { path = "../seam-core" }
