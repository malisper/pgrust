[package]
name = "backend-replication-syncrep-gram"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "backend_replication_syncrep_gram"
path = "src/lib.rs"

[dependencies]
mcx = { path = "../mcx" }
types-error = { path = "../types-error" }
backend-replication-syncrep-scanner-seams = { path = "../backend-replication-syncrep-scanner-seams" }
