[package]
name = "backend-snowball-dict-snowball-seams"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "backend_snowball_dict_snowball_seams"
path = "src/lib.rs"

[dependencies]
seam-core = { path = "../seam-core" }
mcx = { path = "../mcx" }
types-error = { path = "../types-error" }
types-tsearch = { path = "../types-tsearch" }
backend-commands-define-seams = { path = "../backend-commands-define-seams" }
