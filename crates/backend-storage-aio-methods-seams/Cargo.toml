[package]
name = "backend-storage-aio-methods-seams"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "backend_storage_aio_methods_seams"
path = "src/lib.rs"

[dependencies]
seam-core = { path = "../seam-core" }
types-startup = { path = "../types-startup" }
