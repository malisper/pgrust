[package]
name = "backend-storage-file-fd"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "backend_storage_file_fd"
path = "src/lib.rs"

[dependencies]
seam-core = { path = "../seam-core" }
types-core = { path = "../types-core" }
types-datum = { path = "../types-datum" }
types-error = { path = "../types-error" }
types-storage = { path = "../types-storage" }
mcx = { path = "../mcx" }
libc = "0.2"
backend-utils-error = { path = "../backend-utils-error" }
# Seam crates for UNPORTED neighbor calls this family makes.
backend-storage-aio-seams = { path = "../backend-storage-aio-seams" }
backend-storage-ipc-seams = { path = "../backend-storage-ipc-seams" }
# Seam crates this unit OWNS (installs every decl from its own init_seams()).
backend-storage-file-seams = { path = "../backend-storage-file-seams" }
backend-storage-file-fd-seams = { path = "../backend-storage-file-fd-seams" }
