[package]
name = "backend-storage-ipc-latch-seams"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "backend_storage_ipc_latch_seams"
path = "src/lib.rs"

[dependencies]
seam-core = { path = "../seam-core" }
