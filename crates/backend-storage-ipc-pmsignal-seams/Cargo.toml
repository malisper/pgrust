[package]
name = "backend-storage-ipc-pmsignal-seams"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "backend_storage_ipc_pmsignal_seams"
path = "src/lib.rs"

[dependencies]
types-core = { path = "../types-core" }
seam-core = { path = "../seam-core" }
types-error = { path = "../types-error" }
