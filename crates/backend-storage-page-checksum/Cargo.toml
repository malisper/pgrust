[package]
name = "backend-storage-page-checksum"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "backend_storage_page_checksum"
path = "src/lib.rs"

[dependencies]
types = { path = "../types" }
