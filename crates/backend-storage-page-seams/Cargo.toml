[package]
name = "backend-storage-page-seams"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "backend_storage_page_seams"
path = "src/lib.rs"

[dependencies]
seam-core = { path = "../seam-core" }
