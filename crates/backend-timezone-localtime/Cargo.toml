[package]
name = "backend-timezone-localtime"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "backend_timezone_localtime"
path = "src/lib.rs"

[dependencies]
backend-timezone-pgtz-seams = { path = "../backend-timezone-pgtz-seams" }
types-core = { path = "../types-core" }
types-pgtime = { path = "../types-pgtime" }
