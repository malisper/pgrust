[package]
name = "backend-timezone-strftime-seams"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "backend_timezone_strftime_seams"
path = "src/lib.rs"

[dependencies]
seam-core = { path = "../seam-core" }
types-pgtime = { path = "../types-pgtime" }
