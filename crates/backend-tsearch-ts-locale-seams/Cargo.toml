[package]
name = "backend-tsearch-ts-locale-seams"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "backend_tsearch_ts_locale_seams"
path = "src/lib.rs"

[dependencies]
seam-core = { path = "../seam-core" }
