[package]
name = "backend-utils-activity-pgstat-database-seams"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "backend_utils_activity_pgstat_database_seams"
path = "src/lib.rs"

[dependencies]
seam-core = { path = "../seam-core" }
