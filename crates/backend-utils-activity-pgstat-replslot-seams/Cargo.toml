[package]
name = "backend-utils-activity-pgstat-replslot-seams"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "backend_utils_activity_pgstat_replslot_seams"
path = "src/lib.rs"

[dependencies]
seam-core = { path = "../seam-core" }
types-tuple = { path = "../types-tuple" }
