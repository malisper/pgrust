[package]
name = "backend-utils-activity-waitevent-seams"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "backend_utils_activity_waitevent_seams"
path = "src/lib.rs"

[dependencies]
types-error = { path = "../types-error" }
seam-core = { path = "../seam-core" }
types-core = { path = "../types-core" }
