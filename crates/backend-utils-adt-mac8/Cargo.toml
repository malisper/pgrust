[package]
name = "backend-utils-adt-mac8"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "backend_utils_adt_mac8"
path = "src/lib.rs"

[dependencies]
types-error = { path = "../types-error" }
types-network = { path = "../types-network" }
# hash_any / hash_any_extended for hashmacaddr8; owner ported, depend directly.
common-hashfn = { path = "../common-hashfn" }
