[package]
name = "backend-utils-adt-network-seams"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "backend_utils_adt_network_seams"
path = "src/lib.rs"

[dependencies]
seam-core = { path = "../seam-core" }
types-network = { path = "../types-network" }
