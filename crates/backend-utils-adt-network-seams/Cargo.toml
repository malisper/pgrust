[package]
name = "backend-utils-adt-network-seams"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "backend_utils_adt_network_seams"
path = "src/lib.rs"

[dependencies]
seam-core = { path = "../seam-core" }
mcx = { path = "../mcx" }
types-network = { path = "../types-network" }
# Canonical unified `Datum` (the by-reference inet varlena image) for the
# `datum_get_inet_pp` detoast edge.
types-tuple = { path = "../types-tuple" }
types-error = { path = "../types-error" }
