[package]
name = "backend-utils-adt-timestamp-seams"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "backend_utils_adt_timestamp_seams"
path = "src/lib.rs"

[dependencies]
seam-core = { path = "../seam-core" }
types-core = { path = "../types-core" }
mcx = { path = "../mcx" }
types-error = { path = "../types-error" }
