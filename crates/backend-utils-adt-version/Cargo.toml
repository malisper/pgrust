[package]
name = "backend-utils-adt-version"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "backend_utils_adt_version"
path = "src/lib.rs"

[dependencies]
# Carrier types.
types-datum = { path = "../types-datum" }
types-error = { path = "../types-error" }
mcx = { path = "../mcx" }

# `cstring_to_text` (varlena.c) — builds the `text` result `Datum`.
backend-utils-adt-varlena-seams = { path = "../backend-utils-adt-varlena-seams" }
