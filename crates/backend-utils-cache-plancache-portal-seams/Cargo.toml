[package]
name = "backend-utils-cache-plancache-portal-seams"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "backend_utils_cache_plancache_portal_seams"
path = "src/lib.rs"

[dependencies]
seam-core = { path = "../seam-core" }
types-portal = { path = "../types-portal" }
