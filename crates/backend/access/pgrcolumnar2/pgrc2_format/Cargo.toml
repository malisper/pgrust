[package]
name = "pgrc2_format"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "pgrc2_format"
path = "src/lib.rs"

[dependencies]
# Per-section CRCs (spec §5.2/§6.4) ride the C-parity crc32c port (zero-dep).
crc32c = { path = "../../../../port/crc32c" }
