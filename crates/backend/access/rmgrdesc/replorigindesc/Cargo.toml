[package]
name = "replorigindesc"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "replorigindesc"
path = "src/lib.rs"

[dependencies]
mcx = { path = "../../../../_support/mcx" }
types_core = { path = "../../../../_support/types/types_core" }
types_error = { path = "../../../../_support/types/types_error" }
wal = { path = "../../../../_support/types/wal" }
# This crate's own inward seam (replorigin_desc / replorigin_identify slots).
replorigindesc_seams = { path = "../replorigindesc_seams" }
