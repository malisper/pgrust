[package]
name = "smgrdesc"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "smgrdesc"
path = "src/lib.rs"

[dependencies]
mcx = { path = "../../../../_support/mcx" }
types_core = { path = "../../../../_support/types/types_core" }
types_error = { path = "../../../../_support/types/types_error" }
types_storage = { path = "../../../../_support/types/types_storage" }
wal = { path = "../../../../_support/types/wal" }
# This crate's own inward seam (smgr_desc / smgr_identify rm_desc slots).
smgrdesc_seams = { path = "../smgrdesc_seams" }
# relpathperm == relpathbackend(rlocator, INVALID_PROC_NUMBER, forknum); owner
# common/relpath.c unported, so through its per-owner seam.
common_relpath_seams = { path = "../../../../common/common_relpath_seams" }
