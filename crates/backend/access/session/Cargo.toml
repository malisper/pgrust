[package]
name = "session"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "session"
path = "src/lib.rs"

[dependencies]
types_error = { path = "../../../_support/types/types_error" }
session_seams = { path = "../session_seams" }
