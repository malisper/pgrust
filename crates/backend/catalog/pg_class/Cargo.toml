[package]
name = "pg_class"
version = "0.1.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "pg_class"
path = "src/lib.rs"

[dependencies]
types_error = { path = "../../../_support/types/types_error" }
pg_class_seams = { path = "../pg_class_seams" }
