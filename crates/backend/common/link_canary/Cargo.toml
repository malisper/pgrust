[package]
name = "link_canary"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "link_canary"
path = "src/lib.rs"

[dependencies]
link_canary_seams = { path = "../link_canary_seams" }
