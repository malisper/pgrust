[package]
name = "ifaddr"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "ifaddr"
path = "src/lib.rs"

[dependencies]

[target.'cfg(unix)'.dependencies]
libc = "0.2"
