[package]
name = "syslogger_seams"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "syslogger_seams"
path = "src/lib.rs"

[dependencies]
seam_core = { path = "../../../_support/seam/seam_core" }
types_startup = { path = "../../../_support/types/types_startup" }
