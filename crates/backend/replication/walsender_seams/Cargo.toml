[package]
name = "walsender_seams"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "walsender_seams"
path = "src/lib.rs"

[dependencies]
seam_core = { path = "../../../_support/seam_core" }
types_error = { path = "../../../_support/types/types_error" }
repl_gram = { path = "../repl_gram" }
