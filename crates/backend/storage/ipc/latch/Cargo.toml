[package]
name = "latch"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "latch"
path = "src/lib.rs"

[dependencies]
libc = "0.2"
parallel_rt_seams = { path = "../../../access/transam/parallel_rt_seams" }
latch_seams = { path = "../latch_seams" }
# Lazy-vacuum cost-delay WaitLatch/ResetLatch(MyLatch) seams home in
# vacuumlazy-seams; latch.c is their owner.
vacuumlazy_seams = { path = "../../../access/heap/vacuumlazy_seams" }
procarray_seams = { path = "../procarray_seams" }
waiteventset_seams = { path = "../waiteventset_seams" }
lmgr_proc_seams = { path = "../../lmgr/lmgr_proc_seams" }
init_small_seams = { path = "../../../utils/init/init_small_seams" }
types_core = { path = "../../../../_support/types/types_core" }
types_error = { path = "../../../../_support/types/types_error" }
types_pgstat = { path = "../../../../_support/types/types_pgstat" }
types_storage = { path = "../../../../_support/types/types_storage" }
