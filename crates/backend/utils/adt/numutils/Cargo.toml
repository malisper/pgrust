[package]
name = "numutils"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "numutils"
path = "src/lib.rs"

[dependencies]
types_error = { path = "../../../../_support/types/types_error" }
