[package]
name = "queryenvironment_home"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "queryenvironment_home"
path = "src/lib.rs"

[dependencies]
nodes = { path = "../../../../_support/types/nodes" }
