[package]
name = "stack_depth_seams"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "stack_depth_seams"
path = "src/lib.rs"

[dependencies]
seam_core = { path = "../../../../_support/seam/seam_core" }
types_error = { path = "../../../../_support/types/types_error" }
