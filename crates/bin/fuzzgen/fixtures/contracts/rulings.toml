# sitediff rulings ledger (plan §4.2). One [[ruling]] per row; first match
# wins; a ruling covers exactly one plane; every ruled hit is still recorded
# as ruled:<id>. Only cosmetic/HINT rulings carry `expires`. `hits` is
# maintained by `sitediff rulings audit`. Schema: crates/bin/fuzzgen/schemas/rulings.schema.json

[[ruling]]
id = "b1-float-ulp"
plane = "rows"
field = "cell"
a = "^-?\\d+(\\.\\d+)?([eE][-+]?\\d+)?$"
b = "^-?\\d+(\\.\\d+)?([eE][-+]?\\d+)?$"
ref = "B1 float-reassociation ruling: float surfaces compare by ulp, not text (ruled.rs FloatUlp; ulp_tol)"
owner = "fuzzgen ruled.rs (migrated by L0.0)"
created = "2026-09-02"
hits = 0

[[ruling]]
id = "b1-float-agg-soft"
plane = "rows"
field = "cell"
ref = "B1 float-reassociation ruling: order-sensitive float aggregate result columns are ruled-soft (ruled.rs FloatAggSoft; render::soft_float_cols mask)"
owner = "fuzzgen ruled.rs (migrated by L0.0)"
created = "2026-09-02"
hits = 0

[[ruling]]
id = "copy-order"
plane = "rows"
field = "order"
stmt = "COPY"
ref = "COPY order ruling: COPY/heap row order is nondeterministic, non-surface (ruled.rs CopyOrder; multiset-equal only)"
owner = "fuzzgen ruled.rs (migrated by L0.0)"
created = "2026-09-02"
hits = 0

[[ruling]]
id = "explain-counter"
plane = "explain"
field = "counter"
a = "(Sort Method|Memory|Buckets|Batches|Disk|Planning Time|Execution Time):"
stmt = "EXPLAIN"
ref = "EXPLAIN runtime resource counters are implementation state, never compared; plan structure under COSTS OFF still compares strictly (ruled.rs ExplainCounter)"
owner = "fuzzgen ruled.rs (migrated by L0.0)"
created = "2026-09-02"
hits = 0

[[ruling]]
id = "explain-timing"
plane = "explain"
field = "timing"
a = "actual time=\\d+\\.\\d+\\.\\.\\d+\\.\\d+"
stmt = "EXPLAIN"
ref = "EXPLAIN ANALYZE wall-clock timing text is never comparable; opt-in lanes only (gramwalk, --mask-explain-timing); actual rows still compare (ruled.rs ExplainTiming)"
owner = "fuzzgen ruled.rs (migrated by L0.0)"
created = "2026-09-02"
hits = 0

[[ruling]]
id = "explain-planning-buffers"
plane = "explain"
field = "planning-buffers"
a = "^Planning:$"
stmt = "EXPLAIN"
ref = "EXPLAIN TEXT Planning: buffer-usage block presence is session cache state; node-level Buffers and Planning Time still compare (ruled.rs ExplainPlanningBuffers)"
owner = "fuzzgen ruled.rs (migrated by L0.0)"
created = "2026-09-02"
hits = 0

[[ruling]]
id = "xml-config"
plane = "wire:E"
field = "M"
a = "^unsupported XML feature$"
b = "^(?!.*panicked).*$"
ref = "xml build-config ruling (LD1-N1): the pinned C oracle is built without libxml while pgrust dlopens libxml2; a pgrust XX000 on the same statement still escalates (ruled.rs XmlConfig; delete only if the CI cluster oracle builds with libxml, plan Q4)"
owner = "fuzzgen ruled.rs (migrated by L0.0)"
created = "2026-09-02"
hits = 0

[[ruling]]
id = "guc-inventory"
plane = "rows"
field = "count"
ref = "GUC-inventory ruling: pg_settings / SHOW ALL row-count shape only (extra pgrust.* GUCs, retuned defaults; docs/design/env-to-guc.md); GUC values still compare strictly (ruled.rs GucInventory)"
owner = "fuzzgen ruled.rs (migrated by L0.0)"
created = "2026-09-02"
hits = 0

[[ruling]]
id = "instance-config"
plane = "rows"
ref = "round-9 FP-9/FP-9b/FP-10 instance-config ruling: pg_hba_file_rules / pg_ident_file_mappings / pg_file_settings / pg_shmem_allocations / pg_stat_progress_* / pg_stat_activity / pg_locks are instance state; rowset shape only, errors still compare (ruled.rs InstanceConfig)"
owner = "fuzzgen ruled.rs (migrated by L0.0)"
created = "2026-09-02"
hits = 0

[[ruling]]
id = "encoding-carve"
plane = "wire:E"
field = "M"
b = "server encoding .* (is not supported|only UTF8)"
ref = "UTF-8-only server-encoding carve, RATIFIED 2026-08-18 (docs/design/carve-ratifications.md §11): a B-side 0A000 carrying the carve citation carries no oracle signal; exact-message scope (ruled.rs EncodingCarve)"
owner = "fuzzgen ruled.rs (migrated by L0.0)"
created = "2026-09-02"
hits = 0

[[ruling]]
id = "instance-lsn"
plane = "rows"
field = "cell"
a = "^[0-9A-F]+/[0-9A-F]+$"
b = "^[0-9A-F]+/[0-9A-F]+$"
ref = "round-9 instance-LSN ruling: backup-control LSNs (pg_backup_start/stop, pg_switch_wal, pg_create_restore_point) are cluster-local, never comparable (ruled.rs InstanceLsn)"
owner = "fuzzgen ruled.rs (migrated by L0.0)"
created = "2026-09-02"
hits = 0

[[ruling]]
id = "lz4-config"
plane = "wire:E"
field = "M"
a = "^compression method lz4 not supported$"
b = "^(?!.*panicked).*$"
ref = "round-9 lz4 build-config ruling (mirror of xml-config): a --without-lz4 oracle rejects 0A000 where pgrust is lz4-capable; a pgrust XX000 still escalates (ruled.rs Lz4Config)"
owner = "fuzzgen ruled.rs (migrated by L0.0)"
created = "2026-09-02"
hits = 0

[[ruling]]
id = "tid-input-upstream"
plane = "wire:E"
field = "C"
a = "^$"
b = "^22P02$"
ref = "round-9 tid-input ruling (project owner): C 18.x tidin accepts malformed forms like '(0,)'; pgrust matches the upstream-fixed strict behavior; exact-signature scope, B-only 22P02 'invalid input syntax for type tid' (ruled.rs TidInputUpstream)"
owner = "fuzzgen ruled.rs (migrated by L0.0)"
created = "2026-09-02"
hits = 0

[[ruling]]
id = "shared-catalog-tcu"
plane = "wire:E"
field = "M"
b = "^tuple concurrently (updated|deleted)$"
ref = "shared-catalog concurrency ruling (2026-08-21 soak): XX000 'tuple concurrently updated/deleted' on shared-catalog DDL is C's own simple_heap_update race under concurrent drivers; any other XX000 escalates (ruled.rs SharedCatalogTcu)"
owner = "fuzzgen ruled.rs (migrated by L0.0)"
created = "2026-09-02"
hits = 0

[[ruling]]
id = "autoconf-shared-race"
plane = "wire:E"
field = "M"
b = "^could not parse contents of file \"postgresql\\.auto\\.conf\""
stmt = "ALTER SYSTEM"
ref = "round-10 RB-14 autoconf shared-state ruling: postgresql.auto.conf is instance-global; asymmetric F0000 under concurrent batches is write/read interleaving (notes/internal classification notes) (ruled.rs AutoconfSharedRace)"
owner = "fuzzgen ruled.rs (migrated by L0.0)"
created = "2026-09-02"
hits = 0

[[ruling]]
id = "oid-literal"
plane = "rows"
field = "cell"
a = "'\\d{5,}'::oid"
b = "'\\d{5,}'::oid"
ref = "round-7 OID ruling: user-object OIDs (>= 16384) embedded as '<n>'::oid literals in deparse text are masked; all other text compares exactly (ruled.rs OidLiteral)"
owner = "fuzzgen ruled.rs (migrated by L0.0)"
created = "2026-09-02"
hits = 0

[[ruling]]
id = "toast-name"
plane = "rows"
field = "cell"
a = "pg_toast_\\d{5,}"
b = "pg_toast_\\d{5,}"
ref = "round-8 OID ruling: pg_toast_<n> relation names embed the owning table's user-range OID; builtin catalog toast names still compare exactly (ruled.rs ToastName)"
owner = "fuzzgen ruled.rs (migrated by L0.0)"
created = "2026-09-02"
hits = 0

[[ruling]]
id = "binary-udt-oid"
plane = "rows"
field = "cell"
ref = "round-7 OID ruling: embedded user-range type oids in record_send/array_send binary images are masked structurally; every other byte of the image still compares exactly (ruled.rs BinaryUdtOid)"
owner = "fuzzgen ruled.rs (migrated by L0.0)"
created = "2026-09-02"
hits = 0

[[ruling]]
id = "cmp-magnitude"
plane = "rows"
field = "cell"
a = "^-?\\d+$"
b = "^-?\\d+$"
ref = "round-7 cmp-magnitude ruling: C's memcmp-convention *cmp() comparators return arbitrary magnitude; scope is direct *cmp() select-list calls with sign-equal int4 results (ruled.rs CmpMagnitude)"
owner = "fuzzgen ruled.rs (migrated by L0.0)"
created = "2026-09-02"
hits = 0

[[ruling]]
id = "parallel-worker-init"
plane = "wire:E"
field = "M"
b = "^parallel worker failed to initialize$"
ref = "round-7 asymmetric-fault ruling: thread-pause faults hit only the instrumented SUT, so B-only 55000 'parallel worker failed to initialize' is injected scheduling; liveness owned by the parallel canary (ruled.rs ParallelWorkerInit)"
owner = "fuzzgen ruled.rs (migrated by L0.0)"
created = "2026-09-02"
hits = 0

[[ruling]]
id = "fault-stmt-timeout"
plane = "wire:E"
field = "M"
b = "^canceling statement due to statement timeout$"
ref = "round-20 asymmetric-fault ruling: B-only 57014 statement-timeout under the decks' symmetric statement_timeout SETs is injected scheduling; exact-message scope (ruled.rs FaultStmtTimeout)"
owner = "fuzzgen ruled.rs (migrated by L0.0)"
created = "2026-09-02"
hits = 0

[[ruling]]
id = "drop-autovacuum-deadlock"
plane = "wire:E"
field = "M"
b = "^deadlock detected$"
stmt = "DROP TABLE|VACUUM FULL"
ref = "round-20/r21 symmetric-race ruling: DROP TABLE / VACUUM FULL vs autovacuum-ANALYZE ancestor-stats propagation forms a hard lock cycle that 40P01s in C too; B-only visibility is fault-widened timing (ruled.rs DropAutovacuumDeadlock)"
owner = "fuzzgen ruled.rs (migrated by L0.0)"
created = "2026-09-02"
hits = 0

[[ruling]]
id = "role-setting-shared-race"
plane = "wire:E"
field = "M"
b = "pg_db_role_setting_databaseid_rol_index"
stmt = "ALTER ROLE ALL"
ref = "r21 symmetric-race ruling: concurrent ALTER ROLE ALL SET batches race the (0, 0) pg_db_role_setting singleton; C's AlterSetting is scan-then-insert with no unique-violation recovery (ruled.rs RoleSettingSharedRace)"
owner = "fuzzgen ruled.rs (migrated by L0.0)"
created = "2026-09-02"
hits = 0

[[ruling]]
id = "scroll-materialize"
plane = "explain"
field = "shape"
a = "^\\s*->\\s*Materialize$"
stmt = "EXPLAIN DECLARE"
ref = "SCROLL-Materialize ruling (round-9 RB-10, ratified Michael 2026-07-17, notes/se-wave10-integration.md §5 item 2): pgrust omits C's planner Materialize wrap for SCROLL cursors; the diff must vanish once the top-level wrap is stripped from A (ruled.rs ScrollMaterialize)"
owner = "fuzzgen ruled.rs (migrated by L0.0)"
created = "2026-09-02"
hits = 0

[[ruling]]
id = "tie-ordering"
plane = "rows"
field = "order"
ref = "docs/conformance/tie-ordering.md: tie order under an underdetermined ORDER BY; multiset-equal only, never under ordered: total (ruled.rs TieOrder)"
owner = "fuzzgen ruled.rs (migrated by L0.0)"
created = "2026-09-02"
hits = 0
