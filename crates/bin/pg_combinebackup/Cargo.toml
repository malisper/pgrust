[package]
name = "pg_combinebackup"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[[bin]]
name = "pg_combinebackup"
path = "src/main.rs"

[dependencies]
libc = "0.2"
controldata_utils = { path = "../../common/controldata_utils" }
manifest = { path = "../../backend/backup/manifest" }
mcx = { path = "../../_support/mcx" }
parse_manifest = { path = "../../common/parse_manifest" }
types_core = { path = "../../_support/types/types_core" }
types_error = { path = "../../_support/types/types_error" }

[dev-dependencies]
pg_sha2 = { path = "../../common/sha2" }
pgtz_seams = { path = "../../backend/timezone/pgtz_seams" }
