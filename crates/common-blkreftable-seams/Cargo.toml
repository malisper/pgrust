[package]
name = "common-blkreftable-seams"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "common_blkreftable_seams"
path = "src/lib.rs"

[dependencies]
seam-core = { path = "../seam-core" }
types-core = { path = "../types-core" }
types-error = { path = "../types-error" }
types-storage = { path = "../types-storage" }
types-blkreftable = { path = "../types-blkreftable" }
mcx = { path = "../mcx" }
