[package]
name = "common-checksum-helper"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "common_checksum_helper"
path = "src/lib.rs"
crate-type = ["rlib"]

[dependencies]
types-core = { path = "../types-core" }
types-crypto = { path = "../types-crypto" }
port-crc32c = { path = "../port-crc32c" }
port-pgstrcasecmp = { path = "../port-pgstrcasecmp" }
common-cryptohash-seams = { path = "../common-cryptohash-seams" }
