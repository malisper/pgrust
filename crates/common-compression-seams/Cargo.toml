[package]
name = "common-compression-seams"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "common_compression_seams"
path = "src/lib.rs"

[dependencies]
seam-core = { path = "../seam-core" }
types-compression = { path = "../types-compression" }
