[package]
name = "common-config-info-seams"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "common_config_info_seams"
path = "src/lib.rs"

[dependencies]
seam-core = { path = "../seam-core" }
types-misc-more2 = { path = "../types-misc-more2" }
