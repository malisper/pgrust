[package]
name = "common-controldata-utils-seams"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "common_controldata_utils_seams"
path = "src/lib.rs"

[dependencies]
seam-core = { path = "../seam-core" }
types-control = { path = "../types-control" }
types-error = { path = "../types-error" }
