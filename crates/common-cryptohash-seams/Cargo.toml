[package]
name = "common-cryptohash-seams"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "common_cryptohash_seams"
path = "src/lib.rs"

[dependencies]
seam-core = { path = "../seam-core" }
types-core = { path = "../types-core" }
types-crypto = { path = "../types-crypto" }
