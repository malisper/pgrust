[package]
name = "common-encnames-seams"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "common_encnames_seams"
path = "src/lib.rs"

[dependencies]
seam-core = { path = "../seam-core" }
