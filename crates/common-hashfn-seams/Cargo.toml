[package]
name = "common-hashfn-seams"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "common_hashfn_seams"
path = "src/lib.rs"

[dependencies]
seam-core = { path = "../seam-core" }
