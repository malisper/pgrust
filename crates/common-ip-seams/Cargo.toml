[package]
name = "common-ip-seams"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "common_ip_seams"
path = "src/lib.rs"

[dependencies]
seam-core = { path = "../seam-core" }
types-net = { path = "../types-net" }
