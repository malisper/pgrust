[package]
name = "common-parse-manifest-seams"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "common_parse_manifest_seams"
path = "src/lib.rs"

[dependencies]
seam-core = { path = "../seam-core" }
types-error = { path = "../types-error" }
types-parse-manifest = { path = "../types-parse-manifest" }
