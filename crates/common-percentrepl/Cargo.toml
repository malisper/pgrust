[package]
name = "common-percentrepl"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "common_percentrepl"
path = "src/lib.rs"

[dependencies]
mcx = { path = "../mcx" }
types-error = { path = "../types-error" }
