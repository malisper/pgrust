[package]
name = "common-ryu"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "common_ryu"
path = "src/lib.rs"

# Faithful, self-contained port of src/common/{d2s,f2s}.c (the Ryū shortest
# round-trip float serializer). It is a pure deterministic algorithm with no
# PostgreSQL runtime dependencies, so this crate has NO dependencies — matching
# the C, which #includes only the local ryu_common.h / digit_table.h / table
# headers.
[dependencies]
