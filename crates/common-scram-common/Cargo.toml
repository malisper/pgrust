[package]
name = "common-scram-common"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "common_scram_common"
path = "src/lib.rs"

[dependencies]
common-sha2 = { path = "../common-sha2" }
common-prng-base64 = { path = "../common-prng-base64" }
backend-libpq-auth-scram-seams = { path = "../backend-libpq-auth-scram-seams" }
