[package]
name = "common-wait-error-seams"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "common_wait_error_seams"
path = "src/lib.rs"

[dependencies]
seam-core = { path = "../seam-core" }
