[package]
name = "common-wchar-fgram"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "common_wchar"
path = "src/lib.rs"
crate-type = ["rlib"]

[dependencies]
libc = { version = "0.2", default-features = false }
pgrust-pg-ffi = { package = "pgrust-pg-ffi-fgram", path = "../pgrust-pg-ffi-fgram" }
