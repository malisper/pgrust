[package]
name = "archive"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "archive"
path = "src/lib.rs"

[dependencies]
types_error = { path = "../../_support/types/types_error" }
percentrepl = { path = "../percentrepl" }
