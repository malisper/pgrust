[package]
name = "common_relpath_seams"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "common_relpath_seams"
path = "src/lib.rs"

[dependencies]
seam_core = { path = "../../_support/seam/seam_core" }
types_core = { path = "../../_support/types/types_core" }
types_storage = { path = "../../_support/types/types_storage" }
