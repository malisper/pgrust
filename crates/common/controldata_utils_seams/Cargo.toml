[package]
name = "controldata_utils_seams"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "controldata_utils_seams"
path = "src/lib.rs"

[dependencies]
seam_core = { path = "../../_support/seam/seam_core" }
control = { path = "../../_support/types/control" }
types_error = { path = "../../_support/types/types_error" }
