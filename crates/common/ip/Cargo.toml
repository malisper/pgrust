[package]
name = "ip"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "ip"
path = "src/lib.rs"

[dependencies]
ip_seams = { path = "../ip_seams" }
net = { path = "../../_support/types/net" }

[target.'cfg(unix)'.dependencies]
libc = "0.2"
