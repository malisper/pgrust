[package]
name = "scram_common"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "scram_common"
path = "src/lib.rs"

[dependencies]
sha2 = { path = "../sha2" }
prng_base64 = { path = "../prng_base64" }
scram_seams = { path = "../../backend/libpq/auth/scram_seams" }
