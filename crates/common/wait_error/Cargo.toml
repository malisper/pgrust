[package]
name = "common_wait_error"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "common_wait_error"
path = "src/lib.rs"
crate-type = ["rlib"]

[dependencies]
wait_error_seams = { path = "../wait_error_seams" }
libc = "0.2"
wasm-libc-shim = { path = "../../wasm-libc-shim" }
