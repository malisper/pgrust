[package]
name = "contrib-amcheck-verify-nbtree-seams"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "contrib_amcheck_verify_nbtree_seams"
path = "src/lib.rs"

[dependencies]
seam-core = { path = "../seam-core" }
types-core = { path = "../types-core" }
types-error = { path = "../types-error" }
