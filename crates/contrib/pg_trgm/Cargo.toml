[package]
name = "pg_trgm"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "pg_trgm"
path = "src/lib.rs"

[dependencies]
# Boundary / support types:
mcx = { path = "../../_support/mcx" }
datum = { path = "../../_support/types/datum" }
fmgr = { path = "../../_support/types/fmgr" }
types_error = { path = "../../_support/types/types_error" }
types_tuple = { path = "../../_support/types/types_tuple" }
types_guc = { path = "../../_support/types/types_guc" }

# GIN opclass-support call protocol (the generic catalog-driven dispatch):
gin = { path = "../../_support/types/gin" }

# GiST opclass-support call protocol (the generic catalog-driven dispatch):
gist = { path = "../../_support/types/gist" }

# Local-reloptions parse table for the gtrgm_options opclass-options proc:
types_reloptions = { path = "../../_support/types/types_reloptions" }
reloptions_seams = { path = "../../backend/access/common/reloptions_seams" }

# Trigram core services:
crc32c = { path = "../../port/crc32c" }
mbutils = { path = "../../backend/utils/mb/mbutils" }

# Seams (consulted, not owned):
dfmgr_seams = { path = "../../backend/utils/fmgr/dfmgr_seams" }
ts_locale_seams = { path = "../../backend/tsearch/ts_locale_seams" }
formatting_seams = { path = "../../backend/utils/adt/formatting_seams" }
arrayfuncs_seams = { path = "../../backend/utils/adt/arrayfuncs_seams" }

# GUC registration:
misc_guc = { path = "../../backend/utils/misc/misc_guc" }
guc_tables = { path = "../../backend/utils/misc/guc_tables" }
