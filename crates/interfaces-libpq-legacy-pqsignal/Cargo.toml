[package]
name = "interfaces-libpq-legacy-pqsignal"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "interfaces_libpq_legacy_pqsignal"
path = "src/lib.rs"

[dependencies]
libc = "0.2"
types-signal = { path = "../types-signal" }
