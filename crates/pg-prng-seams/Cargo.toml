[package]
name = "pg-prng-seams"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "pg_prng_seams"
path = "src/lib.rs"

[dependencies]
seam-core = { path = "../seam-core" }
