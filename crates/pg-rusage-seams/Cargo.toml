[package]
name = "pg-rusage-seams"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "pg_rusage_seams"
path = "src/lib.rs"

[dependencies]
seam-core = { path = "../seam-core" }
types-rusage = { path = "../types-rusage" }
