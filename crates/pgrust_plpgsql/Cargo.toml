[package]
name = "pgrust_plpgsql"
version = "0.1.0"
edition = "2024"

[dependencies]
pest = "2.7"
pgrust_catalog_data = { path = "../pgrust_catalog_data" }
pgrust_nodes = { path = "../pgrust_nodes" }
pgrust_parser = { path = "../pgrust_parser" }
pgrust_plpgsql_grammar = { path = "../pgrust_plpgsql_grammar" }

