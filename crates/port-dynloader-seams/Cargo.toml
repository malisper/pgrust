[package]
name = "port-dynloader-seams"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "port_dynloader_seams"
path = "src/lib.rs"

[dependencies]
seam-core = { path = "../seam-core" }
types-error = { path = "../types-error" }
types-dfmgr = { path = "../types-dfmgr" }
