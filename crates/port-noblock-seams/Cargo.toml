[package]
name = "port-noblock-seams"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "port_noblock_seams"
path = "src/lib.rs"

[dependencies]
seam-core = { path = "../seam-core" }
types-core = { path = "../types-core" }
