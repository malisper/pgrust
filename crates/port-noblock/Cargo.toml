[package]
name = "port-noblock"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "port_noblock"
path = "src/lib.rs"

[dependencies]
libc = "0.2"
types-core = { path = "../types-core" }
port-noblock-seams = { path = "../port-noblock-seams" }
