[package]
name = "port-pg-crc32c-seams"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "port_pg_crc32c_seams"
path = "src/lib.rs"

[dependencies]
seam-core = { path = "../seam-core" }
