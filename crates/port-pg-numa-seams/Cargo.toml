[package]
name = "port-pg-numa-seams"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "port_pg_numa_seams"
path = "src/lib.rs"

[dependencies]
seam-core = { path = "../seam-core" }
