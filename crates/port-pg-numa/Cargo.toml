[package]
name = "port-pg-numa"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "port_pg_numa"
path = "src/lib.rs"

[dependencies]
port-pg-numa-seams = { path = "../port-pg-numa-seams" }
