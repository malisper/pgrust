[package]
name = "port-pg-strong-random-seams"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "port_pg_strong_random_seams"
path = "src/lib.rs"

[dependencies]
seam-core = { path = "../seam-core" }
