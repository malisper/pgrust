[package]
name = "port-pg-strong-random"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "port_pg_strong_random"
path = "src/lib.rs"

[dependencies]
libc = "0.2"
port-pg-strong-random-seams = { path = "../port-pg-strong-random-seams" }
