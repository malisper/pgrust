[package]
name = "port-pgsleep-seams"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "port_pgsleep_seams"
path = "src/lib.rs"

[dependencies]
seam-core = { path = "../seam-core" }
