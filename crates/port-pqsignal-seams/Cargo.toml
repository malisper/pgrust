[package]
name = "port-pqsignal-seams"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "port_pqsignal_seams"
path = "src/lib.rs"

[dependencies]
seam-core = { path = "../seam-core" }
types-signal = { path = "../types-signal" }
