[package]
name = "crc32c"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "crc32c"
path = "src/lib.rs"

[dependencies]
crc32c_seams = { path = "../crc32c_seams" }
pg_crc32c_seams = { path = "../pg_crc32c_seams" }
hash_small_seams = { path = "../../backend/utils/hash/hash_small_seams" }
