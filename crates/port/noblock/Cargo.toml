[package]
name = "noblock"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "noblock"
path = "src/lib.rs"

[dependencies]
libc = "0.2"
types_core = { path = "../../_support/types/types_core" }
noblock_seams = { path = "../noblock_seams" }
