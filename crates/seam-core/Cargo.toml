[package]
name = "seam-core"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "seam_core"
path = "src/lib.rs"

[features]
default = []
trace-seams = ["dep:pgrust-trace"]

[dependencies]
pgrust-trace = { path = "../pgrust-trace", optional = true }
