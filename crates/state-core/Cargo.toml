[package]
name = "state-core"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "state_core"
path = "src/lib.rs"

[dependencies]
types-core = { path = "../types-core" }
