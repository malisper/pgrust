[package]
name = "types-amapi"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "types_amapi"
path = "src/lib.rs"

[dependencies]
types-tableam = { path = "../types-tableam" }
