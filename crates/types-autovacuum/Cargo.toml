[package]
name = "types-autovacuum"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "types_autovacuum"
path = "src/lib.rs"

[dependencies]
types-core = { path = "../types-core" }
types-reloptions = { path = "../types-reloptions" }
types-vacuum = { path = "../types-vacuum" }
