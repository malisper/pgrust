[package]
name = "types-catalog"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "types_catalog"
path = "src/lib.rs"

[dependencies]
types-core = { path = "../types-core" }
types-tuple = { path = "../types-tuple" }
