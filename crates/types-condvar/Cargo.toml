[package]
name = "types-condvar"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "types_condvar"
path = "src/lib.rs"

[dependencies]
types-storage = { path = "../types-storage" }
