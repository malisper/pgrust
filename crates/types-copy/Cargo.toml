[package]
name = "types-copy"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "types_copy"
path = "src/lib.rs"

[dependencies]
types-core = { path = "../types-core" }
mcx = { path = "../mcx" }
