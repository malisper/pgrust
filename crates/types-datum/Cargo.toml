[package]
name = "types-datum"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "types_datum"
path = "src/lib.rs"

[dependencies]
types-core = { path = "../types-core" }
