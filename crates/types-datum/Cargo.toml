[package]
name = "types-datum"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "types_datum"
path = "src/lib.rs"

[dependencies]
mcx = { path = "../mcx" }
types-core = { path = "../types-core" }
