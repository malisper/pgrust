[package]
name = "types-format-type"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "types_format_type"
path = "src/lib.rs"

[dependencies]
mcx = { path = "../mcx" }
types-core = { path = "../types-core" }
