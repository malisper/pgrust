[package]
name = "types-funccache"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "types_funccache"
path = "src/lib.rs"

[dependencies]
mcx = { path = "../mcx" }
types-core = { path = "../types-core" }
types-tuple = { path = "../types-tuple" }
types-error = { path = "../types-error" }
