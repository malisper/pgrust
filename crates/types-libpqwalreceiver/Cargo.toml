[package]
name = "types-libpqwalreceiver"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "types_libpqwalreceiver"
path = "src/lib.rs"

[dependencies]
types-core = { path = "../types-core" }
