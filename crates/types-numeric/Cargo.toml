[package]
name = "types-numeric"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "types_numeric"
path = "src/lib.rs"

[dependencies]
types-datum = { path = "../types-datum" }
# In-memory NumericVar<'mcx> / aggregate states carry charged digit buffers
# (PgVec<'mcx, NumericDigit>); the lifetime threads through every family.
mcx = { path = "../mcx" }
