[package]
name = "types-numeric"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "types_numeric"
path = "src/lib.rs"

[dependencies]
types-datum = { path = "../types-datum" }
