[package]
name = "types-parse-manifest"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "types_parse_manifest"
path = "src/lib.rs"

[dependencies]
types-core = { path = "../types-core" }
