[package]
name = "types-parsenodes"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "types_parsenodes"
path = "src/lib.rs"

[dependencies]
types-core = { path = "../types-core" }
types-nodes = { path = "../types-nodes" }
