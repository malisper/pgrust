[package]
name = "types-replication-applyparallel"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "types_replication_applyparallel"
path = "src/lib.rs"

[dependencies]
types-core = { path = "../types-core" }
