[package]
name = "types-replication"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "types_replication"
path = "src/lib.rs"

[dependencies]
types-core = { path = "../types-core" }
types-parsenodes = { path = "../types-parsenodes" }
types-datetime = { path = "../types-datetime" }
