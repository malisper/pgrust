[package]
name = "types-scan"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "types_scan"
path = "src/lib.rs"

[dependencies]
types-core = { path = "../types-core" }
types-datum = { path = "../types-datum" }
