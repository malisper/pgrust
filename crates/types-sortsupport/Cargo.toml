[package]
name = "types-sortsupport"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "types_sortsupport"
path = "src/lib.rs"

[dependencies]
types-core = { path = "../types-core" }
mcx = { path = "../mcx" }
