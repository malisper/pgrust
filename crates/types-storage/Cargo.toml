[package]
name = "types-storage"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "types_storage"
path = "src/lib.rs"

[dependencies]
mcx = { path = "../mcx" }
types-core = { path = "../types-core" }
types-error = { path = "../types-error" }
