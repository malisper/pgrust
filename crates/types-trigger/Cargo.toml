[package]
name = "types-trigger"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "types_trigger"
path = "src/lib.rs"

[dependencies]
mcx = { path = "../mcx" }
types-core = { path = "../types-core" }
