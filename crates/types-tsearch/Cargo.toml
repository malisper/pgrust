[package]
name = "types-tsearch"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "types_tsearch"
path = "src/lib.rs"

[dependencies]
types-core = { path = "../types-core" }
mcx = { path = "../mcx" }
