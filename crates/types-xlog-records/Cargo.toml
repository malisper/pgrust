[package]
name = "types-xlog-records"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "types_xlog_records"
path = "src/lib.rs"

[dependencies]
types-core = { path = "../types-core" }
types-storage = { path = "../types-storage" }
types-tuple = { path = "../types-tuple" }
