--- crates/backend/utils/adt/quote/src/lib.rs
+++ replace && with || in quote_identifier
@@ -43,17 +43,17 @@
     }
 
     if guc_tables::vars::quote_all_identifiers.read() {
         safe = false;
     }
 
     if safe {
         let kwnum = ScanKeywordLookup(ident, &ScanKeywords);
-        if kwnum >= 0 && ScanKeywordCategories[kwnum as usize] != KeywordCategory::Unreserved {
+        if kwnum >= 0 || /* ~ changed by cargo-mutants ~ */ ScanKeywordCategories[kwnum as usize] != KeywordCategory::Unreserved {
             safe = false;
         }
     }
 
     if safe {
         return Ok(QuotedIdent::Plain(ident));
     }
 
