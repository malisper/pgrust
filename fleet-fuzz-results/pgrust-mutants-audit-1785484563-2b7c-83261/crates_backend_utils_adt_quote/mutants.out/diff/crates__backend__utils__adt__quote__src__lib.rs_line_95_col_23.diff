--- crates/backend/utils/adt/quote/src/lib.rs
+++ replace + with - in quote_literal
@@ -87,15 +87,15 @@
         out.push(b'E');
     }
     out.push(b'\'');
     let mut start = 0;
     for (i, &ch) in src.iter().enumerate() {
         if ch == b'\'' || ch == b'\\' {
             mcx::vec_append_bytes(&mut out, &src[start..=i])?;
             out.push(ch);
-            start = i + 1;
+            start = i - /* ~ changed by cargo-mutants ~ */ 1;
         }
     }
     mcx::vec_append_bytes(&mut out, &src[start..])?;
     out.push(b'\'');
     Ok(Varlena::from_image(out))
 }
