--- crates/backend/utils/mb/mbutils/src/lib.rs
+++ replace < with <= in pg_eucjp_increment
@@ -1050,17 +1050,17 @@
                 if length != 2 {
                     return false;
                 }
                 for i in (0..=1).rev() {
                     let c2 = charptr[i];
                     if c2 < 0xa1 {
                         charptr[i] = 0xa1;
                         return true;
-                    } else if c2 < 0xfe {
+                    } else if c2 <= /* ~ changed by cargo-mutants ~ */ 0xfe {
                         charptr[i] += 1;
                         return true;
                     }
                 }
                 false
             } else {
                 if c1 > 0x7e {
                     return false;
