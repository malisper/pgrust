--- crates/backend/utils/mb/mbutils/src/lib.rs
+++ replace += with *= in pg_eucjp_increment
@@ -1051,17 +1051,17 @@
                     return false;
                 }
                 for i in (0..=1).rev() {
                     let c2 = charptr[i];
                     if c2 < 0xa1 {
                         charptr[i] = 0xa1;
                         return true;
                     } else if c2 < 0xfe {
-                        charptr[i] += 1;
+                        charptr[i] *= /* ~ changed by cargo-mutants ~ */ 1;
                         return true;
                     }
                 }
                 false
             } else {
                 if c1 > 0x7e {
                     return false;
                 }
