--- crates/backend/utils/mb/mbutils/src/lib.rs
+++ delete - in pg_verify_mbstr_len
@@ -1113,17 +1113,17 @@
             }
             mb_len += 1;
             pos += 1;
             continue;
         }
         let l = pg_encoding_verifymbchar(encoding, remaining);
         if l < 0 {
             if no_error {
-                return Ok(-1);
+                return Ok( /* ~ changed by cargo-mutants ~ */1);
             }
             return Err(report_invalid_encoding(encoding, remaining));
         }
         pos += l as usize;
         mb_len += 1;
     }
     Ok(mb_len)
 }
