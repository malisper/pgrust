--- crates/backend/utils/mb/mbutils/src/lib.rs
+++ replace report_invalid_encoding_db -> Box<PgError> with Box::new(Default::default())
@@ -1190,17 +1190,17 @@
         ))
         .with_sqlstate(ERRCODE_CHARACTER_NOT_IN_REPERTOIRE),
     )
 }
 
 #[track_caller]
 #[cold]
 fn report_invalid_encoding_db(mbstr: &[u8], mblen: i32, len: i32) -> Box<PgError> {
-    report_invalid_encoding_int(database_encoding(), mbstr, mblen, len)
+    Box::new(Default::default()) /* ~ changed by cargo-mutants ~ */
 }
 
 #[cold]
 pub fn report_untranslatable_char(
     src_encoding: pg_enc,
     dest_encoding: pg_enc,
     mbstr: &[u8],
 ) -> Box<PgError> {
