--- crates/backend/utils/mb/mbutils/src/lib.rs
+++ replace conversion_not_supported_fatal -> Box<PgError> with Box::new(Default::default())
@@ -1277,27 +1277,17 @@
         ))
         .with_sqlstate(ERRCODE_FEATURE_NOT_SUPPORTED),
     )
 }
 
 #[track_caller]
 #[cold]
 fn conversion_not_supported_fatal(pending: pg_enc) -> Box<PgError> {
-    Box::new(
-        PgError::new(
-            FATAL,
-            format!(
-                "conversion between {} and {} is not supported",
-                enc_name(pending),
-                GetDatabaseEncodingName()
-            ),
-        )
-        .with_sqlstate(ERRCODE_FEATURE_NOT_SUPPORTED),
-    )
+    Box::new(Default::default()) /* ~ changed by cargo-mutants ~ */
 }
 
 #[track_caller]
 #[cold]
 fn invalid_byte_value_error(b: u8) -> Box<PgError> {
     Box::new(
         PgError::error(format!(
             "invalid byte value for encoding \"{}\": 0x{b:02x}",
