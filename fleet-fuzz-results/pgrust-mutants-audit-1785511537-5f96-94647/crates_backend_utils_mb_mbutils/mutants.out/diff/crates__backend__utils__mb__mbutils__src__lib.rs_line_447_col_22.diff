--- crates/backend/utils/mb/mbutils/src/lib.rs
+++ replace == with != in pg_do_encoding_conversion
@@ -439,17 +439,17 @@
     dest_encoding: pg_enc,
 ) -> PgResult<Option<PgVec<'mcx, u8>>> {
     if src.is_empty() {
         return Ok(None);
     }
     if src_encoding == dest_encoding {
         return Ok(None);
     }
-    if dest_encoding == PG_SQL_ASCII {
+    if dest_encoding != /* ~ changed by cargo-mutants ~ */ PG_SQL_ASCII {
         return Ok(None);
     }
     if src_encoding == PG_SQL_ASCII {
         pg_verify_mbstr(dest_encoding, src, false)?;
         return Ok(None);
     }
 
     if !xact_seams::is_transaction_state::call() {
