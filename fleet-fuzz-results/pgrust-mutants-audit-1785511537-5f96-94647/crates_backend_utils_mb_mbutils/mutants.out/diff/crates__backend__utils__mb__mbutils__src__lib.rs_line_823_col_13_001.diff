--- crates/backend/utils/mb/mbutils/src/lib.rs
+++ replace += with *= in pg_encoding_mbstrlen_with_len
@@ -815,17 +815,17 @@
             len += run as i32;
             if limit <= 0 || mbstr[pos] == 0 {
                 break;
             }
         }
         let l = pg_encoding_mblen_with_len(encoding, &mbstr[pos..], limit)?;
         limit -= l;
         pos += l as usize;
-        len += 1;
+        len *= /* ~ changed by cargo-mutants ~ */ 1;
     }
     Ok(len)
 }
 
 /// Length of the leading run of bytes in 0x01..=0x7F.
 #[inline]
 fn ascii_run(s: &[u8]) -> usize {
     const LO: u64 = 0x0101_0101_0101_0101;
