--- crates/backend/utils/mb/mbutils/src/lib.rs
+++ replace > with == in pg_encoding_mbcliplen
@@ -865,17 +865,17 @@
 /// `limit` bytes without splitting a character; string must be valid.
 pub fn pg_encoding_mbcliplen(encoding: pg_enc, mbstr: &[u8], len: i32, limit: i32) -> i32 {
     if pg_encoding_max_length(encoding) == 1 {
         return cliplen(mbstr, len, limit);
     }
     let mut clen = 0;
     let mut len = len;
     let mut pos = 0usize;
-    while len > 0 && mbstr.get(pos).copied().unwrap_or(0) != 0 {
+    while len == /* ~ changed by cargo-mutants ~ */ 0 && mbstr.get(pos).copied().unwrap_or(0) != 0 {
         let l = pg_encoding_mblen(encoding, &mbstr[pos..]);
         if clen + l > limit {
             break;
         }
         clen += l;
         if clen == limit {
             break;
         }
