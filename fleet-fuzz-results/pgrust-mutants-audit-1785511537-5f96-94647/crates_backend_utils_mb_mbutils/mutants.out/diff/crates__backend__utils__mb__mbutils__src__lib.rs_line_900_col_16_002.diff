--- crates/backend/utils/mb/mbutils/src/lib.rs
+++ replace > with >= in pg_mbcharcliplen
@@ -892,17 +892,17 @@
     }
     let mut clen = 0;
     let mut nch = 0;
     let mut len = len;
     let mut pos = 0usize;
     while len > 0 && mbstr.get(pos).copied().unwrap_or(0) != 0 {
         let l = pg_mblen_with_len(&mbstr[pos..], len)?;
         nch += 1;
-        if nch > limit {
+        if nch >= /* ~ changed by cargo-mutants ~ */ limit {
             break;
         }
         clen += l;
         len -= l;
         pos += l as usize;
     }
     Ok(clen)
 }
