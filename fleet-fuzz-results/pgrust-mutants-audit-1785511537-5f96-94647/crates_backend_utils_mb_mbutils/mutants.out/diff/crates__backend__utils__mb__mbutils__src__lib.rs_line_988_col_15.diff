--- crates/backend/utils/mb/mbutils/src/lib.rs
+++ replace >= with < in pg_utf8_increment
@@ -980,17 +980,17 @@
     if length == 4 && charptr[3] < 0xBF {
         charptr[3] += 1;
         return true;
     }
     if length >= 3 && charptr[2] < 0xBF {
         charptr[2] += 1;
         return true;
     }
-    if length >= 2 {
+    if length < /* ~ changed by cargo-mutants ~ */ 2 {
         let limit = match charptr[0] {
             0xED => 0x9F,
             0xF4 => 0x8F,
             _ => 0xBF,
         };
         if charptr[1] < limit {
             charptr[1] += 1;
             return true;
