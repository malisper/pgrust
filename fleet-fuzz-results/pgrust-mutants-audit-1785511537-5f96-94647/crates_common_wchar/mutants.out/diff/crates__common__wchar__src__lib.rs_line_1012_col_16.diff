--- crates/common/wchar/src/lib.rs
+++ delete - in pg_big5_verifychar
@@ -1004,17 +1004,17 @@
 }
 
 fn pg_big5_verifychar(s: &[u8]) -> i32 {
     let mbl = pg_big5_mblen(s);
     if (s.len() as i32) < mbl {
         return -1;
     }
     if mbl == 2 && nonutf8_invalid_pair(s) {
-        return -1;
+        return  /* ~ changed by cargo-mutants ~ */1;
     }
     for i in 1..mbl as usize {
         if s[i] == 0 {
             return -1;
         }
     }
     mbl
 }
