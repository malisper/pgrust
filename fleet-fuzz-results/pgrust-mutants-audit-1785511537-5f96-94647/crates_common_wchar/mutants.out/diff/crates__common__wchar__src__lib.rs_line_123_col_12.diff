--- crates/common/wchar/src/lib.rs
+++ replace += with -= in pg_ascii2wchar_with_len
@@ -115,17 +115,17 @@
 }
 
 fn pg_ascii2wchar_with_len(from: &[u8], to: &mut [pg_wchar]) -> i32 {
     let mut len = from.len();
     let mut fi = 0;
     let mut ti = 0;
     while len > 0 && from[fi] != 0 {
         to[ti] = from[fi] as pg_wchar;
-        fi += 1;
+        fi -= /* ~ changed by cargo-mutants ~ */ 1;
         ti += 1;
         len -= 1;
     }
     to[ti] = 0;
     ti as i32
 }
 
 fn pg_ascii_mblen(_s: &[u8]) -> i32 {
