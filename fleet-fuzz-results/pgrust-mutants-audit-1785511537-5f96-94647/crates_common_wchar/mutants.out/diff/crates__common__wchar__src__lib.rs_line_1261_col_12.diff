--- crates/common/wchar/src/lib.rs
+++ delete ! in pg_utf8_verifystr
@@ -1253,17 +1253,17 @@
                 if pg_utf_mblen_byte(s[pos]) > 1 {
                     break;
                 }
             }
         }
     }
     let mut rest = &s[pos..];
     while let Some((&b, tail)) = rest.split_first() {
-        if !is_highbit_set(b) {
+        if  /* ~ changed by cargo-mutants ~ */is_highbit_set(b) {
             if b == 0 {
                 break;
             }
             rest = tail;
             continue;
         }
         let l = pg_utf8_verifychar(rest);
         if l == -1 {
