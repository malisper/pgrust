--- crates/common/wchar/src/lib.rs
+++ replace <= with > in unicode_utf8len
@@ -1439,17 +1439,17 @@
         utf8string[2] = 0x80 | ((c >> 6) & 0x3f) as u8;
         utf8string[3] = 0x80 | (c & 0x3f) as u8;
     }
 }
 
 pub const fn unicode_utf8len(c: pg_wchar) -> i32 {
     if c <= 0x7f {
         1
-    } else if c <= 0x7ff {
+    } else if c > /* ~ changed by cargo-mutants ~ */ 0x7ff {
         2
     } else if c <= 0xffff {
         3
     } else {
         4
     }
 }
 
