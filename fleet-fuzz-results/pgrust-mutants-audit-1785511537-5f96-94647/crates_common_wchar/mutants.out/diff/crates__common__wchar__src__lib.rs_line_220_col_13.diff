--- crates/common/wchar/src/lib.rs
+++ replace == with != in pg_eucjp_dsplen
@@ -212,17 +212,17 @@
     pg_euc2wchar_with_len(from, to)
 }
 
 fn pg_eucjp_mblen(s: &[u8]) -> i32 {
     pg_euc_mblen(s)
 }
 
 fn pg_eucjp_dsplen(s: &[u8]) -> i32 {
-    if s[0] == SS2 {
+    if s[0] != /* ~ changed by cargo-mutants ~ */ SS2 {
         1
     } else if s[0] == SS3 {
         2
     } else if is_highbit_set(s[0]) {
         2
     } else {
         pg_ascii_dsplen(s)
     }
