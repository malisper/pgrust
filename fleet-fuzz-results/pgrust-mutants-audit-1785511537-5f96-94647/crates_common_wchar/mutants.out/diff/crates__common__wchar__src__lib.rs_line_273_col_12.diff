--- crates/common/wchar/src/lib.rs
+++ replace += with -= in pg_euccn2wchar_with_len
@@ -265,17 +265,17 @@
             to[ti] |= from[fi] as u32;
             fi += 1;
             len -= 2;
         } else {
             to[ti] = from[fi] as pg_wchar;
             fi += 1;
             len -= 1;
         }
-        ti += 1;
+        ti -= /* ~ changed by cargo-mutants ~ */ 1;
     }
     to[ti] = 0;
     ti as i32
 }
 
 fn pg_euccn_mblen(s: &[u8]) -> i32 {
     if s[0] == SS2 || s[0] == SS3 {
         3
