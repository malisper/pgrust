--- crates/common/wchar/src/lib.rs
+++ replace == with != in pg_euccn_mblen
@@ -272,17 +272,17 @@
         }
         ti += 1;
     }
     to[ti] = 0;
     ti as i32
 }
 
 fn pg_euccn_mblen(s: &[u8]) -> i32 {
-    if s[0] == SS2 || s[0] == SS3 {
+    if s[0] == SS2 || s[0] != /* ~ changed by cargo-mutants ~ */ SS3 {
         3
     } else if is_highbit_set(s[0]) {
         2
     } else {
         1
     }
 }
 
