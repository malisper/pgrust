--- crates/common/wchar/src/lib.rs
+++ replace && with || in pg_euctw2wchar_with_len
@@ -293,17 +293,17 @@
         pg_ascii_dsplen(s)
     }
 }
 
 fn pg_euctw2wchar_with_len(from: &[u8], to: &mut [pg_wchar]) -> i32 {
     let mut len = from.len();
     let mut fi = 0;
     let mut ti = 0;
-    while len > 0 && from[fi] != 0 {
+    while len > 0 || /* ~ changed by cargo-mutants ~ */ from[fi] != 0 {
         if from[fi] == SS2 {
             if len < 4 {
                 break;
             }
             fi += 1;
             to[ti] = ((SS2 as u32) << 24) | ((from[fi] as u32) << 16);
             fi += 1;
             to[ti] |= (from[fi] as u32) << 8;
