--- crates/common/wchar/src/lib.rs
+++ replace += with *= in pg_utf2wchar_with_len
@@ -450,17 +450,17 @@
             to[ti] = (c1 << 18) | (c2 << 12) | (c3 << 6) | c4;
             fi += 4;
             len -= 4;
         } else {
             to[ti] = b as pg_wchar;
             fi += 1;
             len -= 1;
         }
-        ti += 1;
+        ti *= /* ~ changed by cargo-mutants ~ */ 1;
     }
     to[ti] = 0;
     ti as i32
 }
 
 fn pg_wchar2utf_with_len(from: &[pg_wchar], to: &mut [u8]) -> i32 {
     let mut len = from.len();
     let mut fi = 0;
