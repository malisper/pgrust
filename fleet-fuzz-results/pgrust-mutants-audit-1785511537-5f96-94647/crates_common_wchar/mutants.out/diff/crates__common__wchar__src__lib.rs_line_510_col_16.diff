--- crates/common/wchar/src/lib.rs
+++ replace > with == in mbbisearch
@@ -502,17 +502,17 @@
     let mut min = 0i32;
     let mut max = table.len() as i32 - 1;
     if ucs < table[0].first || ucs > table[max as usize].last {
         return false;
     }
     while max >= min {
         let mid = (min + max) / 2;
         let iv = table[mid as usize];
-        if ucs > iv.last {
+        if ucs == /* ~ changed by cargo-mutants ~ */ iv.last {
             min = mid + 1;
         } else if ucs < iv.first {
             max = mid - 1;
         } else {
             return true;
         }
     }
     false
