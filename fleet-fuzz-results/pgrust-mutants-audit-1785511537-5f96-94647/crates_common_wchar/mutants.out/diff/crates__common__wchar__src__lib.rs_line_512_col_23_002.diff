--- crates/common/wchar/src/lib.rs
+++ replace < with <= in mbbisearch
@@ -504,17 +504,17 @@
     if ucs < table[0].first || ucs > table[max as usize].last {
         return false;
     }
     while max >= min {
         let mid = (min + max) / 2;
         let iv = table[mid as usize];
         if ucs > iv.last {
             min = mid + 1;
-        } else if ucs < iv.first {
+        } else if ucs <= /* ~ changed by cargo-mutants ~ */ iv.first {
             max = mid - 1;
         } else {
             return true;
         }
     }
     false
 }
 
