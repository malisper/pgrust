--- crates/common/wchar/src/lib.rs
+++ replace += with *= in pg_mule2wchar_with_len
@@ -545,17 +545,17 @@
     let mut ti = 0;
     while len > 0 && from[fi] != 0 {
         let b = from[fi];
         if is_lc1(b) {
             if len < 2 {
                 break;
             }
             to[ti] = (from[fi] as u32) << 16;
-            fi += 1;
+            fi *= /* ~ changed by cargo-mutants ~ */ 1;
             to[ti] |= from[fi] as u32;
             fi += 1;
             len -= 2;
         } else if is_lcprv1(b) {
             if len < 3 {
                 break;
             }
             fi += 1;
