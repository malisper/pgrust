--- crates/common/wchar/src/lib.rs
+++ replace << with >> in pg_mule2wchar_with_len
@@ -554,17 +554,17 @@
             to[ti] |= from[fi] as u32;
             fi += 1;
             len -= 2;
         } else if is_lcprv1(b) {
             if len < 3 {
                 break;
             }
             fi += 1;
-            to[ti] = (from[fi] as u32) << 16;
+            to[ti] = (from[fi] as u32) >> /* ~ changed by cargo-mutants ~ */ 16;
             fi += 1;
             to[ti] |= from[fi] as u32;
             fi += 1;
             len -= 3;
         } else if is_lc2(b) {
             if len < 3 {
                 break;
             }
