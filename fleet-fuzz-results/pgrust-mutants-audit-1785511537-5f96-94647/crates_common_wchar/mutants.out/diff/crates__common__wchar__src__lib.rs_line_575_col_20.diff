--- crates/common/wchar/src/lib.rs
+++ replace |= with &= in pg_mule2wchar_with_len
@@ -567,17 +567,17 @@
         } else if is_lc2(b) {
             if len < 3 {
                 break;
             }
             to[ti] = (from[fi] as u32) << 16;
             fi += 1;
             to[ti] |= (from[fi] as u32) << 8;
             fi += 1;
-            to[ti] |= from[fi] as u32;
+            to[ti] &= /* ~ changed by cargo-mutants ~ */ from[fi] as u32;
             fi += 1;
             len -= 3;
         } else if is_lcprv2(b) {
             if len < 4 {
                 break;
             }
             fi += 1;
             to[ti] = (from[fi] as u32) << 16;
