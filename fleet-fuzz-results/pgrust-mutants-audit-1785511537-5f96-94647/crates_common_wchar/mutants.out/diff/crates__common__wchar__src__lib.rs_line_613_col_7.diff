--- crates/common/wchar/src/lib.rs
+++ replace == with != in is_lcprv1
@@ -605,17 +605,17 @@
 
 #[inline(always)]
 fn is_lc2(b: u8) -> bool {
     (0x90..=0x99).contains(&b)
 }
 
 #[inline(always)]
 fn is_lcprv1(b: u8) -> bool {
-    b == LCPRV1_A || b == LCPRV1_B
+    b != /* ~ changed by cargo-mutants ~ */ LCPRV1_A || b == LCPRV1_B
 }
 
 #[inline(always)]
 fn is_lcprv2(b: u8) -> bool {
     b == LCPRV2_A || b == LCPRV2_B
 }
 
 fn pg_wchar2mule_with_len(from: &[pg_wchar], to: &mut [u8]) -> i32 {
