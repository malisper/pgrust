--- crates/common/wchar/src/lib.rs
+++ replace += with *= in pg_wchar2mule_with_len
@@ -657,17 +657,17 @@
             ti += 4;
             cnt += 4;
         } else if (0xf5..=0xfe).contains(&lb) {
             to[ti] = LCPRV2_B;
             to[ti + 1] = lb;
             to[ti + 2] = (w >> 8) as u8;
             to[ti + 3] = w as u8;
             ti += 4;
-            cnt += 4;
+            cnt *= /* ~ changed by cargo-mutants ~ */ 4;
         } else {
             to[ti] = w as u8;
             ti += 1;
             cnt += 1;
         }
         fi += 1;
         len -= 1;
     }
