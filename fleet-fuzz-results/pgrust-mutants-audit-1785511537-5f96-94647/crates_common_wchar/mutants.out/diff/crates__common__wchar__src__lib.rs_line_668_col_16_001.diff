--- crates/common/wchar/src/lib.rs
+++ replace += with *= in pg_wchar2mule_with_len
@@ -660,17 +660,17 @@
             to[ti] = LCPRV2_B;
             to[ti + 1] = lb;
             to[ti + 2] = (w >> 8) as u8;
             to[ti + 3] = w as u8;
             ti += 4;
             cnt += 4;
         } else {
             to[ti] = w as u8;
-            ti += 1;
+            ti *= /* ~ changed by cargo-mutants ~ */ 1;
             cnt += 1;
         }
         fi += 1;
         len -= 1;
     }
     to[ti] = 0;
     cnt
 }
