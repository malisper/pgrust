--- crates/common/wchar/src/lib.rs
+++ replace pg_latin12wchar_with_len -> i32 with 1
@@ -701,27 +701,17 @@
     } else if is_lcprv2(b) {
         2
     } else {
         1
     }
 }
 
 fn pg_latin12wchar_with_len(from: &[u8], to: &mut [pg_wchar]) -> i32 {
-    let mut len = from.len();
-    let mut fi = 0;
-    let mut ti = 0;
-    while len > 0 && from[fi] != 0 {
-        to[ti] = from[fi] as pg_wchar;
-        fi += 1;
-        ti += 1;
-        len -= 1;
-    }
-    to[ti] = 0;
-    ti as i32
+    1 /* ~ changed by cargo-mutants ~ */
 }
 
 fn pg_wchar2single_with_len(from: &[pg_wchar], to: &mut [u8]) -> i32 {
     let mut len = from.len();
     let mut fi = 0;
     let mut ti = 0;
     while len > 0 && from[fi] != 0 {
         to[ti] = from[fi] as u8;
