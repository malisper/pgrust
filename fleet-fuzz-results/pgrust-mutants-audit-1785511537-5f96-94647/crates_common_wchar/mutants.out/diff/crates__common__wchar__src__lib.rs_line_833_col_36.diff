--- crates/common/wchar/src/lib.rs
+++ replace == with != in nul_pos
@@ -825,17 +825,17 @@
     if is_highbit_set(s[0]) {
         2
     } else {
         pg_ascii_dsplen(s)
     }
 }
 
 fn nul_pos(s: &[u8]) -> i32 {
-    match s.iter().position(|&b| b == 0) {
+    match s.iter().position(|&b| b != /* ~ changed by cargo-mutants ~ */ 0) {
         Some(p) => p as i32,
         None => s.len() as i32,
     }
 }
 
 fn pg_ascii_verifychar(_s: &[u8]) -> i32 {
     1
 }
