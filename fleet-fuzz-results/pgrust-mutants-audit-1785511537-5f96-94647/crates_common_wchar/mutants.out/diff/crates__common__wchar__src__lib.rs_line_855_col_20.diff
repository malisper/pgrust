--- crates/common/wchar/src/lib.rs
+++ delete - in pg_eucjp_verifychar
@@ -847,17 +847,17 @@
 fn pg_eucjp_verifychar(s: &[u8]) -> i32 {
     let len = s.len();
     let c1 = s[0];
     if c1 == SS2 {
         if len < 2 {
             return -1;
         }
         if !(0xa1..=0xdf).contains(&s[1]) {
-            return -1;
+            return  /* ~ changed by cargo-mutants ~ */1;
         }
         2
     } else if c1 == SS3 {
         if len < 3 {
             return -1;
         }
         if !is_euc_range_valid(s[1]) || !is_euc_range_valid(s[2]) {
             return -1;
