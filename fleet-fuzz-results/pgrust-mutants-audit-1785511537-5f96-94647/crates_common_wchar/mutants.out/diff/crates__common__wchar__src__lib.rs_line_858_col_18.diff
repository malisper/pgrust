--- crates/common/wchar/src/lib.rs
+++ replace == with != in pg_eucjp_verifychar
@@ -850,17 +850,17 @@
     if c1 == SS2 {
         if len < 2 {
             return -1;
         }
         if !(0xa1..=0xdf).contains(&s[1]) {
             return -1;
         }
         2
-    } else if c1 == SS3 {
+    } else if c1 != /* ~ changed by cargo-mutants ~ */ SS3 {
         if len < 3 {
             return -1;
         }
         if !is_euc_range_valid(s[1]) || !is_euc_range_valid(s[2]) {
             return -1;
         }
         3
     } else if is_highbit_set(c1) {
