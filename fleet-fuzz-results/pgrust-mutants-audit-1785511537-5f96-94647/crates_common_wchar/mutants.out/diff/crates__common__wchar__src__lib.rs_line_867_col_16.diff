--- crates/common/wchar/src/lib.rs
+++ replace < with == in pg_eucjp_verifychar
@@ -859,17 +859,17 @@
         if len < 3 {
             return -1;
         }
         if !is_euc_range_valid(s[1]) || !is_euc_range_valid(s[2]) {
             return -1;
         }
         3
     } else if is_highbit_set(c1) {
-        if len < 2 {
+        if len == /* ~ changed by cargo-mutants ~ */ 2 {
             return -1;
         }
         if !is_euc_range_valid(c1) || !is_euc_range_valid(s[1]) {
             return -1;
         }
         2
     } else {
         1
