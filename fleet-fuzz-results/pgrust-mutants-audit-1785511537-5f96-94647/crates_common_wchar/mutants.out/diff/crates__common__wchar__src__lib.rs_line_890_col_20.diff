--- crates/common/wchar/src/lib.rs
+++ delete - in pg_euckr_verifychar
@@ -882,17 +882,17 @@
 
 fn pg_euckr_verifychar(s: &[u8]) -> i32 {
     let c1 = s[0];
     if is_highbit_set(c1) {
         if s.len() < 2 {
             return -1;
         }
         if !is_euc_range_valid(c1) || !is_euc_range_valid(s[1]) {
-            return -1;
+            return  /* ~ changed by cargo-mutants ~ */1;
         }
         2
     } else {
         1
     }
 }
 
 fn pg_euckr_verifystr(s: &[u8]) -> i32 {
