--- crates/common/wchar/src/lib.rs
+++ replace < with <= in pg_euctw_verifychar
@@ -911,17 +911,17 @@
         }
         if !is_euc_range_valid(s[2]) || !is_euc_range_valid(s[3]) {
             return -1;
         }
         4
     } else if c1 == SS3 {
         -1
     } else if is_highbit_set(c1) {
-        if len < 2 {
+        if len <= /* ~ changed by cargo-mutants ~ */ 2 {
             return -1;
         }
         if !is_euc_range_valid(s[1]) {
             return -1;
         }
         2
     } else {
         1
