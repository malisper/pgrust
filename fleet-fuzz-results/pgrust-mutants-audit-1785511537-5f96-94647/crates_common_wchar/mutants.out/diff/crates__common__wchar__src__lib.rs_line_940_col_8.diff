--- crates/common/wchar/src/lib.rs
+++ delete ! in pg_johab_verifychar
@@ -932,17 +932,17 @@
     verify_str(s, pg_euctw_verifychar)
 }
 
 fn pg_johab_verifychar(s: &[u8]) -> i32 {
     let mbl = pg_johab_mblen(s);
     if (s.len() as i32) < mbl {
         return -1;
     }
-    if !is_highbit_set(s[0]) {
+    if  /* ~ changed by cargo-mutants ~ */is_highbit_set(s[0]) {
         return mbl;
     }
     for i in 1..mbl as usize {
         if !is_euc_range_valid(s[i]) {
             return -1;
         }
     }
     mbl
