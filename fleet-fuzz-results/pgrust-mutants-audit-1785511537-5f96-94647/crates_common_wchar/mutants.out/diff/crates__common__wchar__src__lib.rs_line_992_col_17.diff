--- crates/common/wchar/src/lib.rs
+++ delete ! in pg_sjis_verifychar
@@ -984,17 +984,17 @@
     }
     if mbl == 1 {
         return mbl;
     }
     let c1 = s[0];
     let c2 = s[1];
     let head = (0x81..=0x9f).contains(&c1) || (0xe0..=0xfc).contains(&c1);
     let tail = (0x40..=0x7e).contains(&c2) || (0x80..=0xfc).contains(&c2);
-    if !head || !tail {
+    if !head ||  /* ~ changed by cargo-mutants ~ */tail {
         return -1;
     }
     mbl
 }
 
 fn pg_sjis_verifystr(s: &[u8]) -> i32 {
     verify_str(s, pg_sjis_verifychar)
 }
