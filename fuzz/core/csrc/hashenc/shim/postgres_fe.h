/*
 * SHIM postgres_fe.h — NOT PostgreSQL code. See shim/c.h for provenance.
 * The vendored src/common files are compiled -DFRONTEND (their frontend
 * build is byte-identical logic; ALLOC/FREE become malloc/free exactly as
 * in a real frontend libpgcommon build).
 */
#ifndef PG_DIFFFUZZ_HASHENC_SHIM_POSTGRES_FE_H
#define PG_DIFFFUZZ_HASHENC_SHIM_POSTGRES_FE_H

#include "c.h"

#endif
