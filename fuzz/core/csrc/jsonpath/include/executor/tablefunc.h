/* SHIM header for the jsonpath_diff oracle - NOT PostgreSQL code (plumbing only, never logic). */
#ifndef _TABLEFUNC_H
#define _TABLEFUNC_H
typedef struct TableFuncRoutine TableFuncRoutine;	/* opaque here */
#endif
