/* SHIM utils/memutils.h (tsvec oracle) — NOT PostgreSQL code.
 * MaxAllocSize lives in the shim postgres.h (tsvectorrecv bound check);
 * no MemoryContext machinery is reachable in the retained code. */
#ifndef PG_DIFFFUZZ_TSVEC_MEMUTILS_H
#define PG_DIFFFUZZ_TSVEC_MEMUTILS_H
#endif
