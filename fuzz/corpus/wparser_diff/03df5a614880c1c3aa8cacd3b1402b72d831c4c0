;

create index wowidx on test_tsvector using gist (a);

SET enable_seqscan=OFF;
SET enable_indexscan=ON;
SET enable_bitmapscan=OFF;

explain (comt~ off) SELECT count(*) FROM test_tsvector WHERE a @@ 