# Campaign resume point — weekly API limit hit 2026-07-29 (resets Aug 2 5am PT)

Ledger at cutoff: **947 proved / 3,189** (census-verified). All completed
work committed & pushed through 75b08afe96. Nothing verdict-bearing was
lost; in-flight lane state salvaged as WIP (75b08afe96 — re-derive, never
trust).

## Terminated mid-run (relaunch these lanes on reset)
1. geo remainder — partial ext2.rs harnesses; lane was mid box mul/div
   planes + point grids; solver-inversion note (default 181s) unrecorded.
2. numeric N1-N3 — 2461 numeric_send recorded wall(memory 13.5GB symex)
   by the lane; was starting 1703 planes.
3. varbit W10 — "pass 6" of its queue; module doc updates in WIP.
4. aclcheck runner — mid-queue with reordered driver; IsSystemClass
   temp-toast probe UNRESOLVED (divergence candidate).
5. wave-5 runner (scalar-misc/uuid/pg_lsn/hash-rows) — partial.
6. fleet wall-retry — job submitted; found fleet-toolchain crate-hash
   mangle issue, built runqueue-r2 (committed); CHECK S3
   kani-suite/<tip>/pgrust-kani-suite-* for its artifact first.
7. Decode lane (NEVER STARTED): the fleet semantic findings — char
   signedness 6-row cluster (HIGH: possible platform-divergence class),
   float-agg stddev/corr grids (suspect dsqrt dual-mode artifact),
   brin dist_time/timetz overflow rows (staged timetz zone-wrap
   candidate lives there), network/oracle unwind-bound harness debt,
   vacuous control_varchar_clip_skew.
8. SUITE-name repair lane (NEVER STARTED): 342 stale harness names by
   family int-arith 140 / datetime-cmp 79 / typcache-inst 52 /
   pseudotypes 52 / brin-minmax 16 / numeric-probe 3 + 16 controls + 7
   geo ext names; fix from cargo kani list. (mbconv + conv-module
   compile break already fixed in 3e0f47dcfb.)

## Awaiting Michael
- money MIN/-1 ruling (recommend 22003 per int8div + upstream report #3)
- Divergence candidates in pipeline: IsSystemClass temp-toast, \v-trim,
  timetz zone-wrap (= the brin fleet fails?), gist_box_same NaN plane,
  interval_avg FMA contraction, deserialize trailing-bytes, char
  signedness cluster.
- CBMC NAN report (drafted, unfiled); Kani ICE #3 (unfiled); dsqrt
  dual-mode mechanism isolation for both reports.

## Fleet facts
Full-suite runs replicated 2x @3ade6dd7: 1067 pass / 58 efo / 6 real
semantic fails (char cluster) / rest tooling debt. Batches A+B run at tip
994f9977: real finds = brin dist overflow x2, float-agg grids x3 (suspect
artifact), + harness debt. Artifacts under
s3://pgrust-fleet-results-149051628381/kani-suite/ (7-DAY LIFECYCLE —
copy out by Aug 4 or lose the logs; local copies in the session
scratchpad may not survive).

## Run-ready inventory
~530 harnesses / 21 canonical runqueues (all 4-col gated format);
DigitBuf fixed core landed (38f13030d1) — numeric-arithmetic proof lane
is unblocked and unstarted.
