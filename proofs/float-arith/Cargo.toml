[package]
name = "proof_float_arith"
version = "0.1.0"
edition = "2021"

[workspace]

[dependencies]
adt_float = { path = "../../crates/backend/utils/adt/float" }
types_fmgr = { path = "../../crates/_support/types/fmgr" }
types_error = { path = "../../crates/_support/types/types_error" }
datum = { path = "../../crates/_support/types/datum" }
# send harnesses (2026-07-29 wave): real StringInfo/Mcx under the
# proof_support mcx-stubs recipe (int-arith send precedent).
mcx = { path = "../../crates/_support/mcx" }
proof_support = { path = "../support", features = ["mcx-stubs", "tiny-proof-heap"] }
