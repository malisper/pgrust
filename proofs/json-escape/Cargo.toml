[package]
name = "poc-json-escape"
version = "0.1.0"
edition = "2021"

[workspace]

[dependencies]
adt_json = { path = "../../crates/backend/utils/adt/json" }
stringinfo = { path = "../../crates/_support/types/stringinfo" }
mcx = { path = "../../crates/_support/mcx" }
proof_support = { path = "../support", features = ["mcx-stubs", "tiny-proof-heap"] }
# direct dep so #[kani::stub(types_error::PgError::error, ..)] resolves
types_error = { path = "../../crates/_support/types/types_error" }

[build-dependencies]
cc = "1"
