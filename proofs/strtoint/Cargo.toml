[package]
name = "poc-strtoint"
version = "0.1.0"
edition = "2021"

[workspace]

[lib]
path = "src/lib.rs"

[dependencies]
# Prove the SHIPPED port, not a copy.
numutils = { path = "../../crates/backend/utils/adt/numutils" }
types_error = { path = "../../crates/_support/types/types_error" }

[package.metadata.kani.flags]
c-lib = ["c/pg_numutils_vendored.c"]
