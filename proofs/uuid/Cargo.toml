[package]
name = "proof-uuid"
version = "0.1.0"
edition = "2021"

[workspace]

[dependencies]
adt_uuid = { path = "../../crates/backend/utils/adt/uuid" }
# canonical reject-path stubs (PgError::error / format / from_utf8_lossy);
# wave 5 recv/send harnesses add the mcx-stubs recipe
proof_support = { path = "../support", features = ["mcx-stubs", "tiny-proof-heap"] }
# named by the #[kani::stub(types_error::PgError::error, ..)] target paths
types_error = { path = "../../crates/_support/types/types_error" }
# --- wave 5 (recv/send + generate_uuidv7 RNG seam) ---
datum = { path = "../../crates/_support/types/datum" }
types_fmgr = { path = "../../crates/_support/types/fmgr" }
mcx = { path = "../../crates/_support/mcx" }
stringinfo = { path = "../../crates/_support/types/stringinfo" }
# RNG-seam stub target path (kani::stub needs a direct dep)
pg_strong_random = { path = "../../crates/port/pg_strong_random" }
