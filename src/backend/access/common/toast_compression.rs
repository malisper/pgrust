pub use crate::include::access::toast_compression::*;

// :HACK: Compression support will be added after external TOAST storage
// is in place. Keep the PG-style module split so compression stays isolated.
