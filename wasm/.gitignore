# Build artifacts produced by build.sh (~90 MB) — regenerate, don't commit.
assets/postgres.wasm
assets/postgres.wasm.br
assets/postgres.wasm.gz
assets/vfs.img
assets/vfs.img.br
assets/vfs.img.gz
assets/vfs.json
assets/psql.wasm.br
assets/psql.wasm.gz
# Local pre-upload bundle assembled by deploy.sh stage — never committed.
deploy-staging/
# The @pgxsinkit/pglite-opfs-repacked ESM bundle the `--fs broker` lane imports — a symlink to
# another repo's dist output, never a checked-in copy. See wasm/broker-fs.js.
vendor/
